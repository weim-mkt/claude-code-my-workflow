# Qualification Ledger

Every check, gate, and review agent that is allowed to clear work must have a row here.
**A checker with no row is unqualified, and its green light means nothing.**

Written by `/vaccinate`. See [`verification-ladder.md`](../../.claude/references/verification-ladder.md) rung 0.

Verdicts: **PASS** (detects its named class at the stated threshold) · **FAIL** (misses it) ·
**BLOCKED** (could not be run — reason required; never record as PASS).

| Date | Target | Artifact | Defect classes | N | Recall | FPR | Baseline | Verdict |
|---|---|---|---|---|---|---|---|---|
| 2026-08-21 | `check-model-versions.sh` | `README.md` | T1 superseded-model-as-current | 1 | 1/1 | 0/0 on clean control | `grep -c "Opus 4.8"` also detects; gate's value is its allow-marker logic | **PASS** |
| 2026-08-21 | `check-model-versions.sh` (replicate — script unchanged since prior row, per `git log`) | `README.md` | T1, two flavors: superlative + version line · bare superseded version string, no superlative | 2 | 2/2, both localized to `README.md:442` | 0/0 on restored clean control | bare grep false-alarms on allow-marked historical mentions; gate's value remains SSoT-anchor + allow logic | **PASS** |
| 2026-08-21 | `check-skill-integrity.py` | `.claude/skills/capture-environment/SKILL.md` | T3 body invokes a tool absent from `allowed-tools` | 1 | 1/1 | 0/0 on clean control | none simpler (needs frontmatter↔body parse) | **PASS** |

| 2026-08-21 | `scripts/backtest.sh` (all 6 gates) | `README.md` | broken link · stale model · count drift · unfalsifiable superlative | 4 | **3/4** | 0/0 on clean control | n/a (composite) | **FAIL** → fixed, see next row |
| 2026-08-21 | `check-surface-sync.py` count patterns | `README.md` | template-verb count drift ("this template has N skills") | 3 | 3/3 | **0/3** on legitimate-prose controls ("start with 2-3 skills", "7 parallel agents", "clo-author ships 17 specialized agents") | bare `grep -c` would false-positive on all 3 controls | **PASS** |
| 2026-08-21 | `scripts/check-links.py` | `README.md`, `CLAUDE.md` | missing file · unresolvable heading anchor | 2 | 2/2 | 0/0 on clean control | none simpler (needs anchor slugging) | **PASS** |
| 2026-08-21 | `scripts/validate-findings.py` | synthetic findings array | wrapper-not-array · missing field · unknown field · bad enum · non-derived id · duplicate id · line 0 · malformed JSON | 8 | 8/8 | 0/1 (valid finding accepted) | none | **PASS** |

| 2026-08-21 | `scripts/backtest.sh` — **full stress test, all 7 gates** | `README.md`, `CLAUDE.md`, `docs/index.html`, `scripts/`, a skill | count drift (compound) · count drift (template verb) · superseded model · broken link · broken anchor · superlative · stale auto-mode claim · root scratch file · draft copy · undeclared tool | 10 | **10/10** | **0/1** on the clean-repo control | n/a (composite) | **PASS** |
| 2026-08-21 | `check-surface-sync.py` coverage of `docs/index.html` | landing page | count drift on the published page | 1 | 1/1 | 0/0 | n/a | **PASS** (see note) |

## Harness qualification (2026-08-21)

The eval harness is itself a check, so it had to be qualified before any result it produces
counts. Three defects were found by running it, and one of them made every prior number
meaningless:

| Defect | How it presented | Fix |
|---|---|---|
| N=1 replicate | same case scored 3/3 and then 1/3 ten minutes later | default N=3 + a variance gate that exits 3 when sd > 0.5 |
| `nottrigger` written backwards | scored 1/1 whatever happened | `nottrigger-*` cases now invert the assertion |
| single-substring grading | correct answers phrased differently scored as misses | assertions list alternatives; any one counts |
| **the manipulation never happened** | **`--settings skillOverrides`, `Skill(name)` deny rules, and `--disallowedTools Skill` all FAIL to remove a project skill in headless mode.** Both arms had the skill. A clean, low-variance, entirely false result was about to be recorded here. | `--setting-sources user` verified to actually drop project skills; a **mandatory manipulation check now aborts the run** when both arms look identical |

> **The variance gate measured precision, not validity.** Three identical measurements of the
> wrong thing are perfectly precise. Only the positive control — *did the manipulation take?* —
> catches that, and it is now the first thing the harness runs.

| 2026-08-21 | **`scripts/run-skill-eval.sh` (the harness itself)** | negative control: unrelated question | harness must NOT invent a benefit | 1 case × 3 reps | delta **0** (3/3 vs 3/3, sd 0.00) | 0 — no false benefit | n/a | **PASS** |
| 2026-08-21 | **`scripts/run-skill-eval.sh` (the harness itself)** | positive control: a fact present only in `defect-library.md` | harness must detect a real benefit | 1 case × 3 reps | **+2** (6/6 vs 4/6, sd 0.00) | — | baseline cannot know the fact | **PASS** |
| 2026-08-21 | **manipulation check** | skill visibility probe | did the with/without manipulation take? | 2 probes | with=1, without=0 | — | — | **PASS** |

| 2026-08-21 | `check-repo-hygiene.py` | tracked tree | root scratch · `_old` copy · numbered duplicate (sibling) · `_v2` filename · tracked `.aux` · unexpected top-level dir | 6 | 6/6 | **0/2** on controls (numbered pipeline stage; numbered file with no sibling) | `git status` alone sees none of these | **PASS** |
| 2026-08-21 | `check-derived-counts.py` | README/guide/index | wrong journal count · wrong phase count · wrong snippet count · wrong gate count | 4 | 4/4 | 0/0 | bare grep cannot map claim→source-of-truth | **PASS** |
| 2026-08-22 | `check-spec-conformance.py` | temp skill | empty `description:` (the greedy-regex bug had masked it) | 1 | 1/1 | 0/0 on clean control | — | **PASS** |
| 2026-08-21 | `check-staleness.py` | README, guide/docs HTML | stale auto-mode claim · unfalsifiable superlative · hand-edited render · source-without-render · expired SSoT (date-shimmed) | 5 | 5/5 | 0/0 | mtime comparison (the approach it replaced) fails on fresh clones | **PASS** |
| 2026-08-21 | `quality_score.py` | `.qmd` | broken R chunk + placeholder text (auto-fail path) | 1 | 1/1 (exit 2) | 0/1 — clean control scored 100/exit 0 | — | **PASS** |

## Skill evals — not yet run

Distinct from gate qualification. `/vaccinate` asks *can this checker detect a planted defect?*
Evals ask *does this skill produce better work than not having it?* A skill can pass evals and
still be a useless reviewer — well-formed, plausible findings that miss real defects. Run both.

**No skill in this template has been eval'd.** Method and priority order:
[`.claude/skills/vaccinate/evals/README.md`](../../.claude/skills/vaccinate/evals/README.md).

## Not yet qualified

These are relied upon and have **never been measured**. Until they have a row, treat their
output as unverified.

| Target | Why it matters |
|---|---|
| `/review-paper --peer` | used for submission decisions |
| `claim-verifier` (CoVe) | HIGH-WARN gate-refuses `/commit` |
| `/audit-reproducibility` | gates the replication package |
| `/seven-pass-review` | submission-readiness |
| the 18-agent review fleet | every fan-out review |
| `.claude/hooks/notify.sh` | passive convenience hook (Notification) — the failure mode is a missed nudge, not a false green |
| `.claude/hooks/context-monitor.py` | passive (PostToolUse) — context-budget nudge; silence is indistinguishable from "nothing to say" |
| `.claude/hooks/pre-compact.py` | passive (PreCompact) — the save-to-disk checklist before compression |
| `.claude/hooks/post-compact-restore.py` | passive (SessionStart) — re-injects context after compaction |
| `.claude/hooks/log-reminder.py` | passive (Stop) — session-log reminder |
| `.githooks/pre-commit` | entry point that delegates to the gate suite; the delegation is exercised on every commit, its own logic (bypass envs, exit-code roll-up) is unmeasured |

The five hooks above are **passive**: they emit a nudge and never make a decision, so a dead one
costs a reminder rather than a wrong green light. That is why they are debt and not a blocker —
but it is debt, recorded here, not silence. The three hooks that *do* decide
(`root-of-trust-guard.py`, `git-guardrails.py`, `claim-reconcile.py`) are measured below and
re-measured on every backtest run.

## Hook layer qualification (2026-08-23)

A hook is the gate nobody watches. It runs in a subprocess, its output is a decision the harness
consumes, and when it stops working it stops working **silently** — no error, no red gate, just a
session that behaves as though the hook had nothing to say. Two checks now stand behind that:
gate 9 (`check-ledger-coverage.py`) proves every hook is *wired* to a file that exists, and gate
10 (`hook-battery.sh`) proves the wired file still *acts* — it re-seeds each guard's target
failure on every backtest run, so the three guard rows below are re-measured continuously, not
only on the date recorded here.

| Date | Target | Artifact | Defect classes | N | Recall | FPR | Baseline | Verdict |
|---|---|---|---|---|---|---|---|---|
| 2026-08-23 | `check-ledger-coverage.py` | fixture clone outside the repo (scripts, hooks, settings, entry point, ledger; git-tracked) | registered gate that runs with no ledger row · ledger row naming a checker no longer on disk · hook path in settings mistyped to a file that does not exist · debt-section heading reworded (`## Not yet qualified` → `## Unqualified (backlog)`) → CannotRun (exit 2), not a silent promotion of every unmeasured checker · debt heading de-headingified to bold `**Not yet qualified**` → CannotRun | 5 | 5/5 | **0/3** on controls (untouched fixture → exit 0; the same rowless gate declared under 'Not yet qualified' → exit 0 with a VISIBLE DEBT warning; a `## Change log` table backticking a debt hook path with 'qualification pending' → exit 0, the entry STAYS visible debt — name-extraction is scoped to qualification/debt sections, so an incidental mention no longer promotes) | grepping the ledger cannot know what actually runs — the registered set is derived live from the gate runner, the session settings, and the commit entry point | **PASS** |
| 2026-08-23 | `scripts/hook-battery.sh` — **grading the grader** | copied hook directory, selected with `HOOK_DIR` | each of the three decision hooks stubbed to always allow, one at a time — `root-of-trust-guard.py` (73 cases in its own sections), `git-guardrails.py` (77 cases in its own sections), `claim-reconcile.py` (2 cases in its own sections). **Those three gated figures are SECTION TALLIES, not the stub outcomes** (narrowed 2026-08-24): a section letter records which hook a case FIRES, and a stub outcome turns on which hook DECIDES. The two coincide for `root-of-trust-guard.py` and `claim-reconcile.py` and do NOT for `git-guardrails.py`, because `root-of-trust-guard.py` imports it and delegates the destructive-verb deny list to it. The measured outcomes, their composition and the command that reproduces them are in the reproduction note below — the outcome is a number you take from a run, never one you read off this row | 3 | 3/3 (exit 1, the failing cases named) | 0/1 on the real hook directory (234/234 cases, exit 0) | none — nothing else in the repo executes a hook | **PASS** |
| 2026-08-23 | `scripts/hook-battery.sh` — **the battery's own isolation from the git environment it runs in** | a decoy git repository in a temp dir, plus a re-entry of the battery under `--fixture-selftest` with `GIT_DIR` / `GIT_INDEX_FILE` / `GIT_PREFIX` exported at that decoy — the environment git really exports to a hook, absolute, as it arrives from a linked worktree | the battery's fixture `init`/`add`/`commit` following an inherited `GIT_DIR` instead of the fixture: the fixture's commit landing in the inherited repository (e1) · that repository's HEAD moving — a junk `seed` commit on the user's branch (e2) · that repository's working tree left with a phantom deletion (e3) | 3 | **3/3** — measured by deleting the `GIT_*` strip from a copy of the battery and running it: e1 `selftest-fixture-not-clean`, e2 HEAD moved `a76a73b…` → `3a9afbb…`, e3 the decoy reporting a deletion of an `untracked.txt` it never had | 0/0 (the three cases have no separate silent control; the whole battery passing on a clean environment — 234 cases, exit 0 — *is* the control, and cases c27/c28 carry the two-direction control for the same defect in `git-guardrails.py`) | `git -C <fixture>` — which does NOT isolate against an exported `GIT_DIR`, and was the mechanism of the defect | **PASS** |
| 2026-08-24 | `root-of-trust-guard.py` | synthetic PreToolUse events (re-seeded every backtest by the battery) | shell write into the root of trust (`tee` into a settings path with a doubled separator) · redirection wrapped in `bash -c '...'` (the `-c` payload is unwrapped and re-scanned, depth ≤ 2) · `find <hook dir> ... -delete` · command-wrapper-prefixed write — `env -i bash -c '<redirect>'` and a bare `nice rm <hook>` (the wrapper AND its option flags — `env -i`, `nice -n N`, `sudo -u X`, `stdbuf -oL`, `timeout N` — are consumed so the real shell/deleter behind them is reached; before r3 the skip halted at the first flag and both the `-c` unwrap and direct detection were bypassed) · `env -S '<payload>'` / `env --split-string='<payload>'` (and the attached `-S<payload>` form) — env SPLITS the string into words and EXECUTES them, so it is a command-payload carrier like a shell `-c`; the payload is now UNWRAPPED and re-scanned (depth ≤ 2) rather than skipped as an opaque value, as r3 did (r4 fix) · **wrapper-chained + bundled env split-string** — `nice env -S '<redirect>'` / `timeout 5 env -S …` (env located THROUGH the shared skip_wrappers, stopping ON env, so a command-wrapper in front of env no longer hides it) and the bundled short-flag forms `env -vS`/`env -iS` (any short-flag group ending in S is a split-string carrier); before r5 env_split_payload used its own assignment-only scan and a `^-S`-anchored match, so both silently ALLOWED a protected-path write (r5 fix) · **shell-option misparse in the `-c` payload finder** — `bash --rcfile <file> -c '<deleter>'` and the bundled `bash -ic '<deleter>'`; `shell_c_payload` spotted `-c` with the loose test `w.startswith("-") and "c" in w`, so a VALUE-TAKING long option that merely contains a `c` set the flag and the guard returned `<file>` as the payload — the real payload was never re-scanned and its write passed SILENTLY, failing toward MISSING the write, the one direction this guard must not fail in. The shell's own options are now parsed: long options never count as `-c`, `--rcfile`/`--init-file`/`-o` are consumed WITH their values, and only a `c` inside a single-dash short group is the real flag (r6 fix) · **git's own working-tree writers** — `git rm <hook>`, `git checkout [<rev>] -- <settings>`, `git restore [--source=<rev>] <settings>`, `git clean -fd <hook dir>`, `git stash push -- <hook>`, `git mv <hook> <elsewhere>`, and `git -C <protected dir> clean -fd`; the writer tables named cp/mv/rm/ln/sed -i/dd/find/tar but never git, so a single ALLOWED `git checkout HEAD~1 -- .claude/settings.json` restored the root of trust from an arbitrary revision and every hook registration went with it, while the semantically identical `cp`/`rm` spelling was DENIED — the guarantee failed on the choice of writer program alone. This was NOT covered by the disclosed residual (interpreters, pipe-fed deleters, unknown wrappers): the command word is known, the protected path is a literal argument, and the write is direct. A `git` segment's GLOBAL options are now walked to the real subcommand, and only the writer subcommands above have `is_protected()` applied to their path operands, respecting `--` (r7 fix) · **unscoped path matching** — `is_protected()` tested path TEXT with no repository anchoring, so the guard denied writes into ANY tree spelling `.claude/settings*.json` / `.claude/hooks/` / `.githooks/`: a throwaway fixture clone, a second checkout, the user's own `~/.claude`. That protected none of this repository's gates (they are not in those trees) while its deny message asserted the path was "part of THIS repository's root of trust", and it BLOCKED the gate-9 reproduction two rows down, whose seeded defect is a mistyped hook path written into a fixture clone's `settings.json`. A token now counts only when it RESOLVES INSIDE the project (`CLAUDE_PROJECT_DIR` → the git repository the call runs in → the repository the hook ships in; relative tokens resolve against the event's `cwd`, `~`/`$VAR` are expanded, and an unresolvable scope falls back to the old text match and says so in the message). Pinned by a25 — the project's own settings, denied with the project directory named in the reason (r11 fix) · **backslash-newline LINE CONTINUATION read as a command separator** — `_TOKEN` listed `\n` in its separator alternation while its word alternation's escape class (`\\.`) cannot match backslash+newline, so a trailing `\` was consumed as an ordinary word character and the newline behind it ENDED the segment. A command formatted across lines — ordinary readable shell, not an evasion — was therefore scored as TWO commands and the half carrying the protected path was never seen. Executed against the pre-fix guard: `rm -f \`+NL+`.claude/hooks/git-guardrails.py` (with the second line indented) emitted NO decision and the same string through `bash -c` really deleted the guard file (exists before True, after False); `cp /tmp/x \`+NL+`.claude/settings.json` and the `mv` spelling were silent too, while every one-line spelling DENIED — so the guarantee turned on where the author had put a newline, and this class was NOT on the docstring's four-bullet "WHAT IT DOES NOT CATCH" list, which positively claims the direct-writer case. Continuations are now spliced the way the shell splices them, after heredoc bodies are dropped and before anything is tokenised; the splice is backslash-RUN aware, so an escaped `\\` before a newline stays a literal backslash plus a real separator. Pinned by a27 (continued delete) and a28 (continued redirection, which also proves the quote-stripped backstop sees the joined line) (r12 fix) · **the CROSS-HOOK gap — a destructive git op inside a payload this hook unwraps, with no protected path in it** — the two guards split the work and each was right about its own half: this hook UNWRAPS command payloads (`bash -c`, `sh -c`, `env -S`, reached through the shared skip_wrappers, nesting to depth ≤ 2) and judges PATH writes, while `git-guardrails.py` judges DESTRUCTIVE GIT but treats a `-c` payload as opaque. A payload carrying a destructive verb and no protected path fell BETWEEN them — this hook saw no path and returned 0, the sibling never saw the verb — so `bash -c 'git reset --hard'` and `bash -c 'git clean -fdx'` RAN, the second deleting untracked and ignored research data that no reflog holds. A shell wrapper is how a script, a `Makefile`, or a generated command line ordinarily spells things, so "a determined caller was never the threat model" did not excuse it. **Found by the EXTERNAL REFEREE (GPT-5.6 Sol Pro, 2026-08-23 oracle pass, finding 5), which named both failing cases** — neither hook's own battery could have caught it, because each was measured only against its own half, and the gap lived in the seam. The fix ROUTES rather than copies: every payload this hook unwraps is passed to `git_deny_reason()`, IMPORTED from the sibling, so there is one definition of "destructive git" in the repository and the two cannot drift apart. Scope is deliberately narrow — only the DENY LIST is shared, not the clean-tree check (that needs a resolved repository and a live `git status`, and duplicating repository-identity logic is precisely what produced r13's defects), and only UNWRAPPED payloads are routed, so one command never draws two denials. Pinned by a31/a32 (the referee's own two named cases), a33 (`sh -c 'git push --force'` — a THIRD deny-list rule, proving the WHOLE shared list is reachable rather than two hardcoded spellings), a34 (`env -S`), a35/a36 (wrapper-chained), a37 (the r6 shell-option path), a38 (nested, reached at depth 1) (r14 fix) · **SHELL GLOBS AND BRACE GROUPS — a one-character edit flipped every denied spelling to ALLOWED (r15 fix)** — `matches_root_of_trust` compared path SEGMENTS literally against `.claude`, `.githooks`, `settings.json`, `settings.local.json` and `hooks`, so an ordinary shell glob took the token out of the guard's sight while the shell expanded it to exactly the protected file. Measured at 7eee6b2 with the event cwd at this repository — guard verdict, then what the shell expands the token to: `rm -f .claude/hook?/…` ALLOW -> the guard file itself; `rm -f .claude/*/…` ALLOW -> the same; `rm -f .clau*/hooks/…` ALLOW -> the same; `echo x > .claude/settings.jso?` ALLOW -> `.claude/settings.json`; `rm -rf .claude/hook[s]` ALLOW; `rm -f .claude/{hooks,rules}/…` ALLOW; `rm -f .githook?/…` ALLOW -> the commit entry point — while the LITERAL spellings of all three targets DENIED, so the guard was wired and only the metacharacter defeated it. Globbing was on NEITHER disclosed-residual list, and unlike every item that IS on them it failed toward ALLOW: tab completion and shorthand globs are how people spell paths, so this is ordinary usage, not an evasion. **The fix has three parts, and the third is why one round of measurement was not enough:** (i) a segment now matches a protected NAME when `fnmatch` says the shell's expansion could produce it; (ii) BRACE GROUPS are expanded first, because braces are not globs — the shell emits every alternative whether or not it exists; (iii) the LITERAL `.claude`/`.githooks` fast path in `main()`, a substring test standing in front of the whole scan, was the SAME defect one level up — with (i) and (ii) already in place, `.clau*/hooks/…` and `.githook?/…` were STILL silent (measured) until that trigger was widened to "or the line carries a glob metacharacter", which fails toward scanning. The test is TEXTUAL, not a real expansion: `glob.glob` against the event cwd was implemented first and dropped, because it buys nothing the text rule does not (see the dotfile rule) while it would make the verdict depend on what happens to exist at hook time, add filesystem work to every command carrying a `*`, and hand a pathological pattern a way to stall the hook — and text also covers the file that does not exist YET, which is exactly the `> .claude/settings.jso?` case. **The dotfile rule is what keeps the fix from denying everything:** a bare wildcard segment fnmatches every protected name, so without it `rm -f docs/*` would be DENIED — it must not be, because bash without `dotglob` (and Python's glob likewise) never expands a bare `*` to a DOTFILE, while against the ordinary names under `.claude/` a bare wildcard DOES match, since `rm -rf .claude/*` really does delete the hooks. `dotglob`, `extglob` forms and a command whose real cwd differs from the event's are disclosed as the new residual. Pinned by a43-a49, controls a50-a53 · **MULTIPLE `-C` KEPT ONLY THE LAST — the sibling of the same round's git-guardrails defect, found by auditing THIS hook after fixing that one (r15 fix)** — git composes `-C` options left to right, but `git_write_target` overwrote its `dash_c` on each one, so the writer's working directory was read as the bare `hooks`, which no rule protects. Measured at 7eee6b2: `git -C .claude -C hooks clean -fd` was SILENT and really deletes the hook directory, while the byte-equivalent single-selector spelling `git -C .claude/hooks clean -fd` DENIED (a22). The values are folded git's way now — absolute replaces, relative appends, empty is a no-op — and the composed token is what `is_protected` is asked about. Pinned by a54, control a55 · **PARENT-DIRECTORY (`..`) PATH SPELLINGS (r16 fix)** — `normalize()` collapsed `//` and `/./` but not `/../`, and the segment walk read the segments AS WRITTEN, so a path that pivots through an unprotected real child of `.claude/` and climbs back into a protected one was not recognised while the shell resolved it to exactly the gate-defining file. Measured at 7b8848d against a throwaway fixture tree: an `rm -f` whose argument was spelled .claude/rules/../hooks/git-guardrails.py went SILENT and really deleted the guard, an `echo x >` into .claude/rules/../settings.json went SILENT and really rewrote the file, and both literal twins DENIED. `..` is now resolved PURELY LEXICALLY (no filesystem call, by the same choice the glob rule records), and BOTH spellings are walked — the token as written and the one it lands on — because lexical and kernel resolution diverge through a symlinked segment, so the union can only add denials. Pinned by a56-a58, controls a59 (an ordinary `..` outside the root of trust) and a60 (a READ through the spelling a57 denies) · **HEREDOC OPENER MISREAD — a herestring or a quoted `<<WORD` (r16 fix)** — `_HEREDOC` was a quote-blind regex run over raw line text, so `<<` + a bareword matched a bash HERESTRING (`cmd <<< word`) and a `<<WORD` inside a quoted string; either one made the scanner delete every following line as a heredoc BODY before tokenising. Measured at 7b8848d: with line 1 = `tr a-z A-Z <<< hello` or `echo '<<EOF'`, an `rm -f` of `.claude/hooks/git-guardrails.py` and an `echo x >` into `.claude/settings.json` on line 2 both went SILENT, and both DENY when line 1 is `echo hi`; bash runs line 2 either way. The opener is now WALKED with quote state carried across lines and `<<<` consumed whole. Pinned by a61/a62, control a63 (a REAL heredoc body is still dropped as prose) · **THE REGISTERED TIMEOUT (r16 fix)** — a PreToolUse guard says 'deny' by writing a decision, and a hook the harness kills has written nothing, which is what an ALLOW looks like; the registration is therefore pinned against a bound the hook itself declares. Pinned by a64 · **SHELL-COMMENT BLINDNESS IN THE OPENER SCAN — INTRODUCED BY THE r16 HEREDOC FIX ITSELF (r17 fix)** — r16 taught `_heredoc_opener` about quotes and herestrings and disclosed EXACTLY ONE surviving opener misread (an arithmetic left-shift). It left a second, more ordinary one: the walk had no notion of a shell COMMENT, and `strip_heredocs` runs BEFORE any comment-aware tokenising, so an `#` comment that merely NAMED a heredoc opened one and DELETED every following line. This is the r16 fix's own residual list being wrong about what it missed, one character away from the quoted spelling r16 had just closed. Measured 2026-08-24 at c285699 against throwaway fixtures, line 2 carrying the real op: with line 1 = `# heredoc <<EOF` this guard went SILENT on an `rm -f` of `.claude/hooks/git-guardrails.py` and on an `echo x >` into `.claude/settings.json`, and the sibling hook went SILENT on `git reset --hard HEAD~1`, `git clean -fdx`, `git push --force origin main`, `git add -A` and a dirty-tree `git merge main`; with line 1 = `# just a comment` every one of them DENIED. Executed, not simulated: `bash -c` on the two-line form left the guard file ABSENT, so bash runs line 2 while both hooks write nothing — and a hook that writes nothing is what an ALLOW looks like. The trailing-comment spelling (`echo hi # uses <<EOF here`) and the `<<-EOF` / `<<'EOF'` spellings inside a comment were silent too. Fixed by ending the opener walk at an unquoted `#` that BEGINS a word (start of line, or after whitespace, a semicolon, an ampersand, a pipe, or a parenthesis) — the same test the segmenter already applies to raw words, with a deliberately WIDER separator set, because erring toward "this is a comment" makes the scan drop LESS text, which is the safe direction. Both hooks' copies of `_heredoc_opener` stay byte-identical (verified with `difflib`). Pinned by a65 (full-line comment) and a66 (trailing comment), control a67 · **`>&` — THE REDIRECT-BOTH SPELLING THAT WAS IN NO SURFACE AT ALL (r18 fix)** — bash's csh-style `>&<word>` redirects stdout AND stderr to <word> whenever <word> is not a file descriptor: it is `&><word>` under another name, and `&>` was handled while `>&` was in NEITHER redirection surface. `_TOKEN` carried no `>&` alternative, so it tokenised as the op `>` followed by `&`; `&` is a SEPARATOR, so the segment ENDED there and the write target became a bare word at the head of the NEXT segment, past the `idx + 1` lookahead in `scan_segment` — and the quote-naive backstop could not recover it either, because `_REDIR`'s target class `[^\s;|&<>()]+` excludes `&`. Executed 2026-08-24 at e0c4cdb against a throwaway fixture whose `.claude/settings.json` held `{"REAL":"root of trust: 10 gate registrations"}`: `bash -c 'echo CLOBBERED >& .claude/settings.json'` exited 0 with EMPTY stderr and left the file containing exactly `CLOBBERED`, and the same operator aimed at `.claude/hooks/git-guardrails.py` overwrote the sibling guard. Hook verdicts on the real project in the same run — the operator is written out rather than backticked against its target, so the ledger-coverage gate does not read the pair as a checker path: redirect-both, spaced, into the settings file — ALLOW(silent); the ATTACHED spelling of the same — ALLOW(silent); redirect-both into the sibling guard file `.claude/hooks/git-guardrails.py` — ALLOW(silent). Against the controls, all aimed at the same settings path: `&>` DENY, `>` DENY, `>|` DENY. So the verdict turned on one character of spelling, exactly as in the r15 glob class, and this was on NEITHER disclosed-residual list: the command word, the operator and the protected path are all literal on the command line, which is the standard by which r7 ruled the `git rm` gap IN scope. Fixed in BOTH surfaces — `\d?>&` added to `_TOKEN`'s op alternation ahead of the bare `\d?>`, and to `_REDIR` ahead of the bare `>` — with FILE DESCRIPTOR DUPLICATION excluded by `_is_fd_dup_target`, because `>&2` / `1>&2` / `2>&1` / `>&-` name a descriptor, not a path, and denying them would refuse the most ordinary redirection in any shell script. The line-69 docstring enumeration, which positively claims four redirection spellings, now lists the fifth it catches. Pinned by a68 (spaced), a69 (attached), a70 (hook-file target) and a71 (wrapped in `bash -c`, so the payload carrier and the new operator compose), controls a72-a76 · **BASH'S OTHER TWO QUOTE OPENERS, AND THE CASE OF EVERY RESERVED NAME (r19 fix)** — `unquote` handled `'…'`, `"…"` and backslash escapes, the three r10 closed, and left bash's ANSI-C `$'…'` and locale `$"…"` openers open: the `$` is QUOTING SYNTAX, but it survived into the unquoted word, so a protected path spelled that way arrived as `$.claude/settings.json`, whose first segment `$.claude` matches no protected name and carries no glob metacharacter. Separately, `normalize` documented itself as "Case-sensitive by design" — false the moment the repository sits on a case-INSENSITIVE filesystem, which APFS is by default: `command -v RM` resolves, so the uppercase writer really runs, and `.CLAUDE/settings.json` really is the same file. Measured 2026-08-24 at eacc6f2 against a throwaway project fixture, guard verdict then what bash actually did: a redirect into `$'.claude/settings.json'` ALLOW(silent) and the file really rewritten; the locale spelling the same; `rm -f $'.claude/hooks/…'` ALLOW and the guard file really deleted; `> .CLAUDE/settings.json` ALLOW and the real settings file rewritten; `RM -f .claude/hooks/…` ALLOW and the guard really deleted — while every literal, lowercase twin DENIED, so the verdict turned on quoting syntax or one character of case. The fix DECODES the ANSI-C escapes bash decodes (so `$'\x2eclaude/…'`, which carries none of the literal characters of `.claude`, is reached — that also had to get past the literal-substring fast path in `main()`, the r15 defect one level up) and an unrecognised escape deliberately keeps BOTH characters, because a word the decoder INVENTS matches no rule at all. The case fold is UNCONDITIONAL rather than filesystem-probed, by the same choice the glob rule records — the verdict must not depend on what happens to exist at hook time — and its stated cost is a false DENY on a case-SENSITIVE filesystem for a directory literally spelled `.CLAUDE` that is not the root of trust, which is the safe direction. **The quoting defect failed in BOTH directions, which is why a82 is a two-way control:** the surviving `$` also broke project-SCOPE resolution, so an out-of-project write through the same spelling fell back to the bare text match and was FALSELY DENIED — verified, the old hook denies a82 naming a path beginning `$/var/folders/…`. COMMAND SUBSTITUTION as a redirection target (`> $(echo .claude/settings.json)`, and the backtick spelling) is DISCLOSED as new residual rather than closed: it is silent where the literal denies, and closing it would duplicate the `bash -c` unwrap path for a spelling nobody types by accident. Pinned by a77-a80 (quoting, including the escape-encoded path), a84-a87 (case, both protected roots and the writer program), controls a81, a82, a83, a88-a91 | 73 | 73/73 | **0/49** (read of the same file; malformed event → fail-open, exit 0 silent; documented escape hatch → silent; a quoted `echo` that merely documents `> .claude/settings.json` inside a string → silent — the backstop scans a quote-stripped copy, so a redirect mentioned inside quotes is prose, not a command-line write; the same value-taking shell options carrying a HARMLESS payload — `bash --rcfile <f> -c 'ls -la'` → silent, so the r6 parser did not buy its recall with a false deny; and the two r7 controls that keep git READ-ONLY — `git log --oneline -- .claude/settings.json` and `git status --porcelain .claude/hooks` → silent, so adding git to the writer set did not cost the "every READ is allowed" guarantee; and the r11 scope control — the same `sed -i` write aimed at a fixture clone OUTSIDE the project → silent (a26), which is what unblocks the gate-9 reproduction; and the two r12 controls for the continuation splice, which is the direction a splice can fail in — a GENUINE newline with no backslash in front of it must still SEPARATE commands, so a following `cat .claude/settings.json` is not folded into the preceding `rm`'s argument list (a29), and an ESCAPED backslash before a real newline is a literal backslash, not a continuation (a30). a30 is qualified, not decorative: rebuilding the join as the naive one-liner `re.sub(r"\\\n", " ", cmd)` in a copied hook directory turns it red — 1 of the then-95 cases failing, and no other case moves — while the backslash-run-aware regex keeps it green; and the four r14 controls, which are the whole reason the cross-hook recall above is believable: a blunt "deny any unwrapped payload that mentions git" would pass all eight deny cases and fail every one of these — a READ in the same carrier (`bash -c 'git status --porcelain'`, a39), the DRY RUN the deny message itself recommends (`bash -c 'git clean -n'`, a40), a payload with no git in it at all (`bash -c 'echo hello world'`, a41), and the UNWRAPPED spelling of the same op (a42), which is git-guardrails' to judge and must stay silent here so one command never draws two denials); and the four r15 controls for the glob fix, which are what stops it from becoming "deny anything containing a `*`" — a glob naming an UNPROTECTED child of `.claude/` (`rm -f .claude/rule?/…`, a50); a BARE wildcard elsewhere (`rm -f docs/*`, a51), the sharp one, because the naive spelling of this fix denies the most ordinary command in the repository and this case is the whole reason the dotfile rule exists; a READ through the same glob (`cat .claude/hook?/…`, a52), so "every READ is untouched" survives the fix; and the SAME glob aimed at a clone OUTSIDE the project (a53), so r11's project scoping still holds on the glob path; and the r15 control for the composed `-C` (a55) — a chain that composes its way back OUT of the protected tree (`-C .claude` then `-C ../docs`) stays allowed, so following git's fold did not become denying every `-C` chain that mentions `.claude`; and the r17 control for comment-awareness (a67) — a REAL heredoc opener with a TRAILING comment after it must still OPEN, so its body documenting `rm .claude/hooks/x` is still dropped as prose. a67 is the sharp direction, and it is what distinguishes the fix made from the naive one: rebuilding the rule LINE-WISE — "a line containing a `#` opens nothing" instead of "the walk ENDS at a word-initial `#`" — turns exactly a67 and b23 red while leaving a65/a66/b21/b22 green, because there the comment sits AFTER a real opener. Measured in a copied hook directory on 2026-08-24; and **r18's five controls for the `>&` operator** — FILE-DESCRIPTOR DUPLICATION is not a file write and stays silent in all three spellings (a72 `>&2`, a73 `1>&2`, a74 `2>&1` on a READ of the protected directory, which also proves the backstop skips fd targets and not only the tokenizer path); a `>&` aimed at an UNPROTECTED path inside the same project stays allowed (a75), so the operator was added rather than a blanket deny on `>&`; and a `>&` into a protected path that appears only INSIDE QUOTES writes nothing and stays allowed (a76), so the a7 prose rule survives the new operator. a72 is the sharp direction: a fix that reads every `>&` as a file write refuses the most ordinary redirection in any shell script); and **r19's controls, which are what keep two widening fixes from becoming a blanket deny** — a `$'…'` string that is genuinely just TEXT stays silent (a83), a READ through the new ANSI-C spelling stays silent (a81), an UNPROTECTED path that merely differs in case stays silent (a88/a89), a bare wildcard elsewhere is untouched (a90/a91), and a82 fires in BOTH directions: the same ANSI-C spelling aimed OUTSIDE the project stays allowed, which the pre-fix hook got WRONG by denying it; **r20's three classes, two of which were the third and fourth defect out of one shortcut** — ORDINARY QUOTING INSIDE THE PROTECTED NAME ITSELF (a92-a97): the case-folded literal fast path standing in front of the whole scan was defeated by a backslash or a quote placed *inside* the directory name, so `rm -f .clau\de/hooks/git-guardrails.py`, `rm -f .cl'aud'e/…`, `rm -f .cl"aud"e/…`, `echo X > .clau\de/settings.json` and `rm -f .githook\s/pre-commit` were all ALLOW(silent) while every literal twin DENIED — and bash really deleted the guard file, because those characters are removed before the kernel ever sees the word (measured by the dispatcher at `727a66c` on a throwaway project fixture). That was the THIRD defect out of the same shortcut, after the r15 glob class and the r19 case class, so it is closed as a CLASS and not as three spellings: the trigger is now taken over a copy of the line with backslash, single quote and double quote DELETED as well as as written, and a97 pins the same spelling nested inside a `bash -c` payload, so the closure does not depend on nesting depth. Controls a98-a102 are what keep a widened trigger from becoming a wider deny — quoting in an unprotected path, a READ through the newly-recognised spelling, the same spelling aimed OUTSIDE the project, and a102, the sharp one, where the denoised form LOOKS protected and the trigger duly fires but the scan still allows it, because the real word names nothing protected and a widened trigger cannot manufacture a deny; THE SIBLING LOADER'S DOCUMENTED FAIL-OPEN DID NOT HOLD (a103, a108): its docstring said it fails open on anything, it caught `Exception`, and `SystemExit` is not one. Because the sibling is exec'd IN THIS PROCESS, a `sys.exit(0)` at the sibling's import time terminated the whole guard with exit 0 and EMPTY stdout — which is byte-for-byte what an ALLOW looks like — so a gate-file write became a silent allow (verified by the dispatcher with a stub sibling of `import sys; sys.exit(0)`). Both in-process foreign-code entry points, the loader and the `git_deny_reason` call, now catch `BaseException`; a104-a107 and a109 hold the lines the fix must not cross, including a107, the honest half — with an unloadable sibling the cross-hook rule has no deny list to consult and the wrapped git op IS allowed, recorded as a fact rather than left to be discovered; and THE ROOT-OF-TRUST HALF OF THE LINE-CONTINUATION SPLICE (a110), a continuation inside the protected directory name, whose git-guardrails twin is the row below and whose controls a111/a112 are what a repair gluing the halves unconditionally would have turned red | permissions ship in bypass mode here, so nothing else stands between an agent and the gate definitions | **PASS** |
| 2026-08-24 | `git-guardrails.py` | synthetic PreToolUse events + a throwaway git repo built in a temp dir | `git reset --hard` · history operation started on a dirty tree · quoted `git -C "<path>" merge` on a dirty tree (the `-C` path is honoured, not stripped as a quoted span) · `git commit -m x && git merge` on a dirty tree (commit is NOT a cleaner — it leaves untracked/unstaged work behind — so the trailing history op still denies) · `git stash push -m x && git stash pop && git merge` on a dirty tree (a `pop`/`apply` AFTER a `push` restores the changes — the tree is dirty again by the time the merge runs — so it still denies; before r3 the clean-tree flag latched monotonically on the push and never reconsidered the pop) · pathspec-limited `git stash push -- <path> && git merge` on a dirty tree (a `--`/pathspec/`-p` push stashes only some paths and leaves the rest dirty, so it is NOT a full-tree cleaner and the trailing merge still denies; r3 treated any push as a full cleaner and false-ALLOWED this — r4 fix) · **deny-on-doubt stash classification** — `git stash --keep-index`/`-k` (staged work retained), `git stash -p` bare (only selected hunks stashed), and `git stash push --some-future-flag` (a flag this code has never seen) all `&& git merge` on a dirty tree still DENY; r4 ENUMERATED the partial forms and DEFAULTED unknown/keep-index/bare-`-p` to a full cleaner, so each was a fresh false-ALLOW — `_stash_effect` now returns 'clean' ONLY for an ALLOWLIST of full-cleaner flags (message/`-u`/`-a`/`-q`) and 'partial' for everything else, making a false-ALLOW impossible regardless of future stash flags (r5 fix) · **deny-on-doubt applied at EVERY level, not just the flag scan** (r6 fix — r5 hardened the flag dimension and the same leak returned one level up, three times over): (i) the stash SUBCOMMAND table was still an enumeration of bad cases and had `branch` in the "touches nothing" set, although `git stash branch <name>` APPLIES the stash to the new working tree — `git stash push -m wip && git stash branch tmp && git merge` was a plain, documented, non-interactive false-ALLOW; the three subcommand sets are now allowlists (`pop`/`apply`/`branch` re-dirty, `list`/`show`/`drop`/`clear`/`create`/`store` inert, `push`/`save` the only candidate cleaners) and ANY OTHER subcommand — a future git verb, a typo — returns `'unknown'`, which both fails to clean AND clears a mark an earlier cleaner earned; (ii) the SEGMENT LOOP had the identical defect a level up, reasoning only about stash/commit segments and letting every other segment leave cleanliness untouched, so `git stash push -m wip && touch new.R && git merge` ran the merge on a dirty tree — only a provably-inert segment (an allowlist of read-only git subcommands and shell no-ops) now preserves the mark; (iii) the clean mark was a single repo-agnostic boolean while `-C` is resolved per segment, so `git -C <other checkout> stash && git merge` let a stash in one repository license a merge in another — the mark is now keyed by resolved worktree · **redirection defeats inertness** (r7 fix — the same shape one dimension over, for the third round running): r6's inert-segment allowlist decided inertness from the segment's COMMAND WORD alone and never inspected its REDIRECTIONS, so `git stash push -m wip && echo note > b.txt && git merge`, `… && git log --oneline > CHANGES.txt && git merge` and `… && echo x >> a.txt && git pull` all swept the tree clean, RE-DIRTIED it through a redirection an allowlisted command word carried, and still reached the history op with the clean mark intact — a false-ALLOW of a merge over uncommitted work (executed in a fixture: after the stash `git status --porcelain` is empty, after `echo note > b.txt` it reports b.txt as modified, and the merge completed rc=0). A segment carrying ANY output-redirection operator (truncating, appending, merged-fd, or clobbering) or a `tee` is now NEVER inert, whatever its command word; the allowlist survives only for segments that redirect nothing · **r8 — THE SIMULATOR WAS REMOVED, NOT PATCHED (the defect class is the design, not any one gap).** Everything above r8 is the history of a machine that tried to PREDICT, from command TEXT, whether an earlier segment had left the tree clean enough to license a later `merge`/`rebase`/`pull`. Five rounds hardened five dimensions of that prediction and round 8 found three more leaks at once: (i) a COMMAND SUBSTITUTION inside an allowlisted "inert" word — `git stash push -m wip && echo $(touch new1.R) && git merge` was ALLOWED and left `?? new1.R` in the tree at merge time, as was the backtick spelling; (ii) `cd <other repo> && git merge`, since repository identity was read only from `-C`/cwd, so the guard licensed or refused a merge in a tree it never looked at; (iii) the plain semantic fact that `git stash` / `stash push -m <msg>` does NOT stash UNTRACKED files, so a flag combination the code called a "PROVABLE full-tree cleaner" left `git status --porcelain` non-empty — the guard returned DENY for a bare merge and ALLOW for `stash push -m wip && merge` on the IDENTICAL tree state. Simulating shell semantics by enumeration does not converge, so the machinery that existed only to model intervening segments was DELETED: `_stash_effect`, `_STASH_REDIRTY`/`_STASH_NEUTRAL`/`_STASH_CLEANERS`/`_STASH_CLEAN_FLAGS`/`_STASH_VALUE_FLAGS`, `_segment_is_inert`, `_segment_writes_via_redirection`, `_INERT_GIT_SUBS`/`_INERT_COMMANDS`/`_REDIRECTORS`, `_repo_key`/`_resolve_repo_key`/`_REPO_KEY_CACHE`, and the per-repository `cleaned` map. *(An earlier draft of this row reported the deletion as "633 → 406 lines". Both halves are withdrawn: a `wc -l` of the shipped guard returned **405**, not 406, and 633 cannot be recomputed by any command because the pre-deletion revision was never committed. A size difference against an unverifiable baseline is not a measured number, so the claim is dropped rather than restated. What is checkable is the deleted-symbol list above, each name greppable and absent.)* The rule became what this row then called a CLOSED WHOLE-COMMAND ALLOWLIST containing no prediction — **both halves of that description are SUPERSEDED at r13, see the r13 entry below: the heading was an overclaim (an unidentified invocation passes outside the rule, it is not allowed by it) and the `--autostash` clause was false** — (1) no history op as an actual command → silent; (2) an escape or self-managing op — `--abort`/`--continue`/`--skip`/`--quit`/`--edit-todo`/`--autostash` — → ALLOW without reading the tree, and `--no-autostash` does NOT qualify; (3) otherwise read `git status --porcelain` LIVE in the directory the invocation itself names (event cwd, or a `-C` carried by the history-op segment) and DENY if it is non-empty, regardless of what any other segment in the chain claims to do. Command substitution, `cd`, untracked-after-stash and every shell form not yet invented now fail toward DENY BY CONSTRUCTION, because none of them can make a live `git status` report a dirty tree as clean. **The accepted cost, recorded here as a cost, not as a win:** `git stash push -m x && git merge` is now DENIED although it would have worked — old battery cases c4/c7/c9/c22 flip ALLOW→DENY, and old cases c7–c15 and c20–c22 were DELETED because they tested only the removed simulator. One extra keystroke (run the cleaning step and the history op as SEPARATE commands, which the guard re-checks) traded for closing an unbounded soundness hole; new case c4 pins the flip so it is not "fixed" back · **r9 — THE SIMULATOR STAYED GONE; THE PARSER DID NOT HOLD.** Round 9 attacked the r8 rule directly and found no way to make a dirty tree read as CLEAN — that half held. What it broke is the small amount of PARSING the rule still needs, and every hole skipped the tree reading entirely, which is the same false-ALLOW by another route. Five classes, each executed against a dirty fixture: (i) **rule 2 was a substring search** over the joined, already-unquoted segment, so an exempt token sitting inside an option VALUE exempted a real op — `git merge -m "wip --autostash later" feature-x`, `git pull -m "after the --abort we retry" origin main`, a ref called `feature--continue`, and `git rebase --skip-smudge main` (the trailing `\b` was satisfied by a hyphen) were all ALLOWED where the bare spelling DENIED; the exemption is now exact TOKEN equality, taken after value-taking options' values are skipped and stopping at `--`; (ii) **a trailing shell COMMENT** put the same tokens on the invocation — `git pull origin main  # finish the --continue later` — so `#`-initial words are now dropped by the tokenizer, tested on the RAW word so a quoted `"#123 fix"` message stays an argument; (iii) **a segment was tested for git by its FIRST WORD only**, so every shell compound and grouping form hid the op — `if true; then git merge x; fi`, `( git merge x )`, `(git merge x)`, `{ git merge x; }`, `for d in .; do git -C $d pull; done`, `while true; do git pull; break; done`, `else git rebase main`, `! git merge x`, `>/dev/null git merge x`, and `git status && ( git merge x )` were SILENT while the byte-identical bare command DENIED (executed: the merge completed rc=0 and left the uncommitted file untouched beside it); leading shell syntax is now stripped and a `git` word is searched for ALONG the segment, which also CLOSES the r8-disclosed bare-command-wrapper residual (`timeout 300 git pull`, `nice git merge`); (iv) **git's GLOBAL options were enumerated — seven of them — in TWO places that disagreed** (the `_GO` prefix on every deny regex, and the walk feeding the clean-tree check), so one unlisted option defeated the ENTIRE deny list and the dirty-tree check at once: with `-P` (git's own short spelling of `--no-pager`, whose LONG spelling was listed), `--no-optional-locks`, `--literal-pathspecs`, `--icase-pathspecs`, `--no-replace-objects`, `--exec-path=…` or `--bare` inserted, `reset --hard`, `clean -fd`, `push --force`, `add -A`, `checkout -- .`, `merge`, `pull` and `rebase` ALL went silent; there is now ONE table, listing only the options that take a SEPARATE VALUE, with any other dash-word consumed as a one-token global — so an option this code has never seen fails toward FINDING the subcommand; (v) **a relative `-C` was resolved against the HOOK PROCESS's cwd**, not the event's, so with the two differing `git -C . merge feature-x` in a dirty repo A was judged against the clean repo B the hook happened to sit in — using the exact spelling the docstring recommends as the remedy for the `cd` residual; `-C` is now resolved against the event cwd, and a named directory that cannot be read at all (missing, unexpanded `$var`, git cannot answer) degrades to reading the event cwd instead of allowing on an unanswered question · **r10 — THE OTHER HALF OF THE GUARD WAS STILL READING RAW TEXT.** Rounds 4-9 all attacked the clean-tree check; the DESTRUCTIVE-OP DENY LIST beside it was never ported, and it was still five regexes over the raw `tool_input.command` string. Ordinary shell quoting of ONE token defeated all of them. Executed against the shipped guard (fixture outside the repo, no `ALLOW_DIRTY_MERGE`): `git reset --hard HEAD~1` DENY / `git reset "--hard" HEAD~1` **SILENT**; `git 'reset' --hard HEAD~1` SILENT; `git add -A` DENY / `git add '-A'` SILENT; `git add --all` DENY / `git add "--all"` SILENT; `git add .` DENY / `git add "."` SILENT; `git checkout -- .` DENY / `git checkout -- '.'` SILENT; `git restore .` DENY / `git restore "."` SILENT — seven byte-equivalent spellings, silent, on `main` as well as here, so it was inherited rather than introduced. It was also UNDISCLOSED: the docstring's "WHAT IT DOES NOT SEE" residual block was explicitly scoped to the clean-tree check, and this ledger row named `git reset --hard` as a measured defect class with no mention that a pair of quotes evaded it — a guard trusted past its coverage. CLOSED, not disclosed: the deny list now runs over the SAME tokenised, unquoted segments the clean-tree check already builds (`_strip_heredocs` → `_segments`/`_unquote` → `_strip_shell_head` → `_git_segment` → `_walk_git_globals`), and each rule is a predicate over the ARGUMENT TOKENS of its own subcommand rather than a substring of the command line, so a quoted flag is matched exactly like a bare one. The regex copy of the git-globals table (`_GO`) is DELETED with them, which also removes the "two consumers that disagreed" shape that produced defect (iv) of r9. Two side effects, both measured: a fully QUOTED MENTION — `echo "never run git reset --hard here"` — used to DENY although it writes nothing, and is now correctly silent (new control b13); and an UNQUOTED mention now denies, which is the same accepted over-deny the clean-tree check has carried since r9 and is what covers the bare-wrapper class (`timeout 300 git push --force`). `git restore --staged .` (an unstage, not a working-tree discard) is excluded by predicate rather than by accident of regex anchoring, and stays allowed · **r11 — THE GUARD READ THE INHERITED REPOSITORY, NOT THE ONE IT WAS ASKED ABOUT (found by `/blast-radius`, not by this battery).** `tree_is_dirty()` shelled out as `subprocess.run(["git", "status", "--porcelain"], cwd=cwd)` with **no `env=`**. git EXPORTS its repository into every hook it runs — `GIT_DIR`, `GIT_INDEX_FILE`, `GIT_WORK_TREE`, `GIT_COMMON_DIR`, `GIT_OBJECT_DIRECTORY`, `GIT_PREFIX`, `GIT_NAMESPACE` — absolute when the command came from a linked worktree; `GIT_DIR` overrides repository DISCOVERY, so `cwd=` was advisory and the guard answered about whatever repository the environment named. Wrong in both directions: a dirty tree can read clean (false ALLOW) and a clean tree can read dirty (false DENY). **The consequence for this ledger is worse than either defect: cases c1–c26 were green FOR THE WRONG REASON** whenever the battery ran inside git's hook environment — which is every backtest run from a linked worktree, since gate 10 runs inside `.githooks/pre-commit` — satisfied by reading the session's own dirty repository instead of their fixture. Those twenty-six cases are **re-qualified after the fix**, and their qualification is no longer environment-dependent: the read now runs under an environment stripped of every `GIT_*` variable except `GIT_EXEC_PATH`, by PREFIX rather than by a fixed list, so a git version that adds one more repository-scoping variable cannot quietly re-open it. Two new cases pin both directions — c27 (event cwd DIRTY, environment naming a CLEAN repo → DENY) and c28 (event cwd CLEAN, environment naming the DIRTY one → SILENT), the second of which over-denying cannot satisfy. Measured against the unfixed guard: c27 emitted no decision and c28 emitted a deny — both red, both green after. Recorded as a cost of the measurement, not a win: the environment must name the other repository COMPLETELY (`GIT_WORK_TREE` as well as `GIT_DIR`) for the two readings to disagree at all; with `GIT_DIR` alone git still takes the working tree from the process cwd, and a first draft of c27/c28 written that way passed against the unfixed guard · **r12 — A BACKSLASH-NEWLINE LINE CONTINUATION WAS READ AS A COMMAND SEPARATOR, by BOTH checks at once.** `_SEG_TOKEN` listed `\n` in its `sep` alternation while its `word` alternation's escape class (`\\.`) cannot match backslash+newline, so a trailing `\` was consumed as an ordinary word character and the newline ENDED the segment. In POSIX shell that newline JOINS the line, so one command was scored as two and whichever half carried the destructive flag — or the subcommand itself — was invisible. This is not an exotic evasion: it is how a long command is formatted for readability. Executed against the pre-fix guard on a dirty fixture, all SILENT while every one-line spelling DENIED: `git clean \`+NL+`-xfd` (run through `bash -c` it deleted the untracked file — `junk.txt` before True, after False), `git push \`+NL+`--force origin main`, `git reset \`+NL+`--hard HEAD~1`, `git add \`+NL+`-A`, `git add \`+NL+`--all`, `git checkout \`+NL+`-- .`, `git restore \`+NL+`.`, and — the clean-tree half — `git \`+NL+`merge feature` and `git \`+NL+`pull origin main`, where the `git` segment had no subcommand at all so rule 1 answered "not a history op" and the tree was never read. The docstring's catch-all ("ANY SHELL FORM THAT PUTS THE OP SOMEWHERE THIS PARSER DOES NOT LOOK … Assume there are more") disclosed the CLASS; this is a concrete instance of it and is CLOSED rather than left disclosed, because it is a plain shell form with the op in literal text. Continuations are now spliced — after heredoc bodies are dropped, before segmentation — for both checks, and the splice is backslash-RUN aware so an escaped `\\` before a newline remains a literal backslash plus a REAL separator. Pinned by b15/b16 (deny list) and c29 (clean-tree check) · **r13 — THE CHECK RAN AT THE WRONG MOMENT, AND ELEVEN IN-HOUSE ROUNDS MISSED IT BECAUSE THEY ONLY EVER TESTED CHAINS IN ONE DIRECTION (found by an INDEPENDENT FRONTIER-MODEL REFEREE, 2026-08-23, adjudicating the release; the TOCTOU class and findings 1-3 below are its credit, not this battery's).** The hook decides at `PreToolUse` — BEFORE bash runs the command — so on a CLEAN repository `printf '\n# local work\n' >> analysis.R && git merge main` reads clean at hook time and is DIRTY when git executes. Rounds 4-12 therefore DENIED the chain that CLEANED the tree (`git stash push -m x && git merge`, pinned as c4 and defended as an accepted cost) while ALLOWING the chain that DIRTIED it: the entire false-deny cost was being paid for none of the safety, and the docstring's high-level summary and law 20 both asserted the stronger guarantee (“history operations start from a clean tree”) that the check did not deliver. Disclosing the race further down the same docstring did not repair the summary. Four corrections, all four the referee's, each pinned and each qualified by reverting it in a copied hook directory: **(i) STANDALONE OR NOTHING (rule 0)** — an identified history op now reaches the tree reading only as a standalone simple command; a second shell segment, a pipeline, a redirection, a command or process substitution, a grouping, a `cd`, or a variable assignment DENIES without reading the tree. This is the referee's “cheaper fallback” and, unlike the pre-r13 best-effort recognizer, it is a genuinely closed grammar over the text the parser sees: no ordering of write-then-op survives it, because the accepted shape has nowhere to put a write. It also CLOSES the r8-disclosed `cd <other repo> && git merge` residual by construction. Pinned c31 (the referee's exact failing case, on a CLEAN tree), c33 (pipeline), c34 (command substitution), c35 (an escape flag does not license a chain), controls c32/c36. **(ii) `--autostash` IS NO LONGER UNCONDITIONALLY EXEMPT** — the exemption rested on the written claim that “git ESTABLISHES the precondition this check exists to enforce”, which the referee reproduced as FALSE for untracked files: in a repo holding `?? note.txt`, `git merge --autostash main` completed and the untracked file was still there afterwards, because `git stash` does not stash untracked files — the exact semantic that justified deleting the shell predictor at r8, reintroduced by the exemption beside it. Of the referee's two options the NARROWER one was taken: `--autostash` now reads the tree and is allowed only when porcelain status reports no `??` entry. Case c9 FLIPS `expect_silent` → `expect_deny`; c42 is the new control proving the exemption was narrowed rather than deleted. **(iii) AN UNRESOLVED REPOSITORY SELECTOR NOW DENIES** — r9 had an unreadable `-C` target “degrade to reading the event cwd rather than allowing on an unanswered question”, which is precisely allowing on the unanswered question: with `REPO=/tmp/B`, `git -C "$REPO" merge main` fired from a clean `/tmp/A` read A, liked it, and allowed. It denies, naming the selector. Pinned c37 (unexpanded variable, message content checked) and c38 (missing directory), control c39. **(iv) THE OTHER REPOSITORY SELECTORS ARE NAMED AND DENIED** — the docstring's “the event cwd, or a `-C`” was simply wrong about git, and the referee reproduced both native forms: `git --git-dir=/tmp/B/.git --work-tree=/tmp/B merge feature` launched from clean `/tmp/A` fast-forwarded B and left B's untracked file in place, and `GIT_DIR=… GIT_WORK_TREE=… git merge feature` does the same with no option at all. `--git-dir`/`--work-tree`/`--namespace`/`--bare`, and ANY `NAME=VALUE` assignment on the op's command line, now deny — the ban is on the SHAPE, so a future `GIT_*` variable cannot re-open it. Pinned c40/c41. **The docstring's own overclaim is withdrawn with them (referee finding 4):** the heading “THE RULE IS A CLOSED ALLOWLIST OVER THE WHOLE COMMAND” stood forty lines above this file's own admission that `bash -c 'git merge main'` is not inspected. It now says what is true — a best-effort RECOGNIZER wrapped around a closed decision, with unidentified invocations passing outside the rule entirely — and the “one extra keystroke” framing of the accepted cost is corrected to what it is: an extra tool call, hook cycle and agent round trip. **Seeded qualification, run 2026-08-23:** each of the four corrections reverted in turn in a copied hook directory, battery run with `HOOK_DIR` at the copy — rule 0 reverted turns exactly c31/c33/c34/c35/c40/c41 red; the unconditional `--autostash` exemption restored turns exactly c9 red; the event-cwd fallback restored turns exactly c37/c38 red; the selector and assignment checks removed turn exactly c40/c41 red. No other case moves under any of the four · **r15 — THE READING ITSELF WAS THE HOLE.** r13 fixed WHEN the tree is read and WHICH repository is named. r15 is about the reading: the question git was asked could be reconfigured by the thing being guarded, the directory it was asked about could be mis-composed, and a question that came back UNANSWERED was scored as a pass. Three classes, all executed. **(i) `git status --porcelain` HONOURS CONFIGURATION.** The query carried no `--untracked-files`, so it obeys `status.showUntrackedFiles` — the documented remedy for a slow status on a large worktree. Measured on git 2.50.1 in a repository holding one untracked file with `status.showUntrackedFiles=no`: the guard's own query answers `''`, while `--untracked-files=normal` answers `?? note.txt`. The guard read that tree as CLEAN and ALLOWED both a plain `git merge` and a `git merge --autostash` — reproducing verbatim the hole r13 narrowed `--autostash` to close, since untracked files are the entire reason for that narrowing, and satisfying BOTH directions of the c9/c42 control because neither case sets the config. **Found by an external referee.** Auditing the same call surfaced a SECOND configuration of the same class, which the referee did not name: `diff.ignoreSubmodules=all` (and `submodule.<name>.ignore`) hides a submodule holding modified work — measured ` M sub` by default, `''` with the setting, ` M sub` again with `--ignore-submodules=none`. Both flags are part of the query now, so a repository cannot reconfigure the reading that guards it; the `GIT_*` environment (`GIT_CONFIG_GLOBAL`, `GIT_CONFIG_COUNT`, `GIT_DIR`, …) was already stripped at r11, and `root-of-trust-guard.py` shells out to git nowhere at all, so this one call is the whole of the exposure. Pinned c43/c44/c46, control c45. **(ii) MULTIPLE `-C` COMPOSE, AND THE GUARD KEPT ONLY THE LAST.** git(1): "If multiple -C options are given, each subsequent non-absolute -C <path> is interpreted relative to the preceding -C <path>." `_walk_git_globals` kept the last value and `_resolve_dash_c` resolved it against the EVENT cwd, so a trailing RELATIVE `-C` discarded a leading absolute one and the guard judged a different repository from the one git chdirs into. Measured: from a clean checkout, `git -C <dirty> -C . merge feature` was ALLOWED while real git resolves to <dirty>; and the mirror direction FALSE-DENIED the same shape naming a clean repository from a dirty cwd. The values are folded git's way now — an absolute value replaces the accumulator, a relative one is appended, an empty one is a no-op. Pinned c47/c49, controls c48/c50. **(iii) AN UNREADABLE STATUS WAS AN ALLOW.** `read_status` returned the same `None` for a timeout, a non-zero exit and "not a repository", and the caller with no `-C` read that as the benign cause and CONTINUED. So a status that merely ran SLOWLY — a large worktree on a cold cache, a network filesystem, and note that the configuration in (i) is the documented remedy for exactly that slowness — was a silent ALLOW of a history op over uncommitted work, one line below the `-C` branch r13 had fixed for being the same mistake. Reproduced deterministically with a `git` on PATH that sleeps past the bound, and with one that exits 128. The benign cause is now decided from the FILESYSTEM (a `.git` at or above the directory), so a broken or shimmed git cannot claim it; everything else DENIES, naming the reason; and the 3s bound is raised to 10s and made configurable (`CLAUDE_GIT_STATUS_TIMEOUT`), because deny-on-unreadable turns a genuinely slow worktree into a blocked session. Pinned c51/c52, control c53. **Seeded qualification, run 2026-08-23** — each revert applied to a COPY of the hook directory, battery run with `HOOK_DIR` at the copy, both directions measured: un-pinning the status query turns exactly c43, c44 and c46 red; keeping only the last `-C` turns exactly c47, c48 and c49 red (c48 is a FALSE DENY, which is why over-denying cannot satisfy it); making an unreadable status allow again turns exactly c51 and c52 red. Restoring each seed returns its copy to ALL PASS, and no other case moves under any of the three. c50 does not move under any revert, and is not meant to: it rules out the ALTERNATIVE fix the finding offered — DENY any invocation carrying more than one `-C` — which would pass c47/c49 and fail c48/c50 · **HEREDOC OPENER MISREAD — a herestring or a quoted `<<WORD` (r16 fix)** — this hook carried the SAME quote-blind `_HEREDOC` regex, and `_strip_heredocs` runs FIRST in both `git_deny_reason` and `dirty_tree_reason`, so one such line silenced the UNCONDITIONAL deny list and the clean-tree precondition at once. Measured at 7b8848d on a dirty fixture repo: after `tr a-z A-Z <<< hello` or `echo '<<EOF'`, `git reset --hard HEAD~1`, `git clean -fd`, `git push --force origin main`, `git add -A` and `git merge main` all went SILENT; every one DENIES when line 1 is `echo hi`. Pinned by b18/b19 and c54, control b20 (a REAL heredoc body carrying a force-push is still dropped as prose) · **THE INTERNAL BUDGET EXCEEDED THE REGISTERED TIMEOUT (r16 fix)** — r15 raised `_STATUS_TIMEOUT_DEFAULT` to 10 s so a slow worktree would not wedge a session, while `.claude/settings.json` still registered the hook with `"timeout": 5`. On the exact case the r15 entry (iii) names — a large worktree on a cold cache, a network filesystem — the harness killed the hook before its deny branch could run, and a killed hook emits NOTHING, which is indistinguishable from an allow. Measured at 7b8848d against a dirty fixture with a `git` shim that sleeps 30: unbounded, 10 s, exit 0, `"permissionDecision": "deny"`; under `timeout -s KILL 5`, 5 s, exit 137, ZERO bytes of stdout. The registration is 20 s now, the internal ceiling is derived from it (registration minus a 5 s margin for the rest of the hook's work) and is STRICTLY smaller, and `CLAUDE_GIT_STATUS_TIMEOUT` is CLAMPED rather than honoured — the deny message used to advise raising it, which moved the user further from a deny. Pinned by c55 (the deny still RETURNS inside the registered bound with a stalled git and the shipped internal default) and c56 (the two numbers, and the clamp, checked against `.claude/settings.json` itself). c51 could not see this: it shortens the internal bound with `CLAUDE_GIT_STATUS_TIMEOUT=1` and fires the hook with no harness bound at all · **SHELL-COMMENT BLINDNESS IN THE OPENER SCAN — INTRODUCED BY THE r16 HEREDOC FIX ITSELF (r17 fix)** — the sibling of the root-of-trust class two rows up, in this hook's byte-identical copy of `_heredoc_opener`, and here it silenced the UNCONDITIONAL deny list AND the clean-tree precondition at once, because `_strip_heredocs` runs first in both `git_deny_reason` and `dirty_tree_reason`. Measured 2026-08-24 at c285699 on a dirty fixture: with line 1 = `# heredoc <<EOF`, `git reset --hard HEAD~1`, a destructive `clean -fdx`, `git push --force origin main`, `git add -A` and `git merge main` ALL went SILENT; every one DENIES when line 1 is a comment carrying no `<<`, and bash runs line 2 in each. Same fix, same byte-identical function. Pinned by b21 (full-line comment) and b22 (trailing comment), control b23 · **THE `-C` FOLD WAS LEXICAL WHERE GIT'S IS THE KERNEL'S (r18 fix)** — r15 fixed WHICH `-C` wins; `_resolve_dash_c` then resolved each one with `os.path.normpath`, which pops a `..` off the path as WRITTEN, while git folds `-C` by `chdir()` and the kernel pops a `..` off the path RESOLVED — the physical parent of a symlink's TARGET, not of the link. The two diverge the instant a `..` follows a symlinked component, and the function's own docstring claimed it read "the directory git will chdir into". Executed 2026-08-24 at e0c4cdb. Fixture: a CLEAN repository C holding a TRACKED symlink `link` -> D/sub, where D is a second repository whose porcelain reads ` M sub/f.txt` and which carries a `feature` branch. Ground truth in that fixture — `git -C link/.. rev-parse --show-toplevel` fired from C prints D, while `os.path.normpath(os.path.join(C, 'link/..'))` prints C. Hook fired with event cwd = C on `git -C link/.. merge feature`: ALLOW, silent, zero bytes on stdout. The same command then run through real bash: rc 0, `Updating b558893..4fe1bdd / Fast-forward`, D's HEAD advanced to the feature commit, `incoming.txt` created in D, D's porcelain STILL ` M sub/f.txt` — the uncommitted work now sitting on top of merged history — and C's HEAD untouched, which is what proves the guard read the wrong tree. `-C ./link/..`, `-C link/../`, `-C link/../.` and the composed `-C . -C link/..` all reached the same false ALLOW, while the byte-equivalent `-C <D absolute>` DENIED and a plain `git merge feature` at C was correctly silent — so the verdict turned on whether the path was spelled through the symlink. **The ATTACHED spelling `-Clink/..` stood in that enumeration until r18 and is WITHDRAWN from it: git does not accept an attached `-C<path>` at all.** Measured on the git this row cites (2.50.1, Apple Git-155): `git -C . status --porcelain` exits 0, while `git -C. status --porcelain` exits 129 with `unknown option: -C.` and the usage block. A spelling git refuses to parse never reaches a verdict, so listing it overstated the demonstrated recall class by one form. The battery KEEPS that case, as a DEFENSIVE CONTROL rather than as recall — it pins the guard's handling of a form git itself rejects, and would become recall only if some future git started accepting it. This is NOT the disclosed `cd` residual (there is no `cd` on the line, and rule 0 denies `cd` outright) and NOT an unresolvable selector (it resolves — to the wrong repository). **Fixed by making the fold kernel-faithful** (`os.path.realpath` on each step, absolute and relative alike) rather than by denying on the divergence. Why resolving on disk is acceptable HERE, when the sibling hook deliberately refuses to: `root-of-trust-guard.py` judges paths that need not exist yet, so it resolves `..` LEXICALLY and handles this same divergence by walking BOTH spellings and taking the union of denials — an option that does not exist here, because this guard must pick ONE directory and read a tree in it, and a union of two readings is not a tree state. The objection does not apply either: rule 3's entire verdict is already a live `git status` RUN in the resolved directory, so this branch depends on the filesystem at hook time by construction, and a `-C` naming a directory that does not exist is not a merge git will perform. The cost is the ordinary TOCTOU one the module docstring already discloses for concurrent writers — a symlink repointed between the hook and the exec was read at its old target — and `realpath`'s lexical fallback for components that do not exist lands outside a repository, where the unresolvable-`-C` branch DENIES. The same change gives `_resolve_dash_c`'s `None` one meaning, 'cannot be resolved', and the caller no longer falls through to `read(None)`, which would have read the HOOK PROCESS's own directory — the r9 defect one dimension over. The docstring now asserts only what the code does. Pinned by c57 (the auditor's failing case), c58-c60 (the `./`-prefixed, trailing-`.` and attached spellings — c60 is the defensive control noted above, not recall), c61 (composed with a leading `-C .`), controls c62-c64 · **THE SAME TWO CLASSES, AND HERE THEY SILENCE BOTH HALVES OF THE HOOK AT ONCE (r19 fix)** — `_unquote` had the identical gap, and because the deny list and the clean-tree precondition decide over the SAME tokenised, unquoted segments, one spelling silenced both. Measured 2026-08-24 at eacc6f2 in a fixture repository whose porcelain was non-empty: `git reset $'--hard' HEAD` ALLOW(silent), `git clean $'-fd'` ALLOW, `git reset $"--hard"` ALLOW, and `git $'merge' other` ALLOW — the last is the clean-tree half, permitted on a genuinely dirty tree — while every literal twin DENIED. The decoder matters and is pinned separately: bash DECODES `$'\x2dA'` to `-A`, so stripping quotes without decoding would still miss (b27). The case class is the sharper of the two here, because `_git_segment` identified the command word with an exact `basename == "git"`: `command -v GIT` resolves to `/usr/bin/GIT` on this filesystem, so `GIT reset --hard` runs git while the parser finds NO git segment at all — no deny-list rule and no clean-tree reading, the same double failure the heredoc classes produced. Executed: `GIT reset --hard HEAD` returned 0 and left `git status --porcelain` EMPTY, the uncommitted modification discarded under an ALLOW. Both folds are unconditional, for the reasons in the sibling row. Pinned by b24-b28 (quoting, including the decoder case), b29/b30 (case, program word and title case), c65 (an ANSI-C history VERB on a dirty tree) and c66 (the case-varied program word on a dirty tree), controls b31-b33, c67 (the same uppercase invocation in a CLEAN repository stays allowed, so the verdict is still the live tree reading rather than the spelling) and c68 | 77 | 77/77 | **0/29** (`push --force-with-lease` allowed; `git merge --abort` on a dirty tree allowed; `git rebase --continue` on a dirty tree allowed — rule 2 on a second op and a second escape flag; the same merge on a CLEAN tree allowed; ~~`git merge --autostash` on a dirty tree allowed~~ **WITHDRAWN at r13** — that entry rested on the false claim that git's autostash establishes the precondition; it does not for untracked files, the referee reproduced it, and the case (c9) now DENIES. What survives is the narrower control c42, listed below; a `git merge` merely MENTIONED inside a quoted `echo` allowed — rule 1, the control that stops the new rule from buying its recall by denying everything; and **r9's control for the token-exemption fix** — `git merge --autostash -m "keep --skip in the message" feature-x` allowed, so making the exemption exact-token did not start denying a REAL escape flag that happens to sit beside a message naming one; and **r10's two controls for the tokenised deny list** — `git push "--force-with-lease" origin main` allowed, so quoting the SAFE flag did not start denying it either, and `echo "never run git reset --hard here"` allowed, the false deny the raw regex used to emit on a string that writes nothing; and **r11's control for the environment fix** — with `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE` exported at the DIRTY fixture, a merge whose event cwd is a CLEAN repository is allowed, so reading the directory the guard was ASKED about was not bought by denying more; and **r12's two controls for the continuation splice** — the continued SAFE spelling `git push \`+NL+`--force-with-lease origin main` stays allowed (b17), so splicing did not buy recall by denying every multi-line push, and a continued `git merge \`+NL+`--autostash feature-x` on a dirty tree stays exempt (c30). c30 discriminates in BOTH directions and is the sharper of the two: with the continuation unspliced the exempt token lands in a SEPARATE segment, so the merge segment reads as unexempt and DENIES — measured, it is one of the six cases that go red when the fix is reverted, a FALSE refusal of git's own stash-operate-restore; and **r13's four controls**, which are what stops rule 0 and the narrowed `--autostash` from being satisfied by a guard that simply denies more — the SAME op as a STANDALONE simple command on the same CLEAN tree that c31 denies is allowed (c32); shell metacharacters INSIDE a quoted option value are text, not syntax, so `git merge -m "wip && later"` stays allowed (c36) and the rule-0 scan did not regress to substring matching; a `-C` that DOES resolve, at a clean repository, is still allowed (c39), so denying unresolved selectors did not become denying every `-C`; and `--autostash` over dirt that is one MODIFIED TRACKED file with no `??` entry is still allowed (c42), which is the case that distinguishes the correction actually made from the alternative of deleting the exemption outright. c19 and c30 moved to the same tracked-dirt fixture at r13 for the same reason); and **r15's four controls** — the SAME `status.showUntrackedFiles=no` configuration on a genuinely CLEAN repository stays allowed (c45), so pinning the query did not become refusing every repository that carries the setting; a composed `-C` landing in a CLEAN repository stays allowed although the EVENT cwd is dirty (c48), the direction over-denying cannot satisfy and the one that was FALSE-DENIED before the fix; an ABSOLUTE second `-C` REPLACES the first rather than composing with it (c50), which is what distinguishes the fold git actually performs from a blanket refusal of multi-`-C` invocations; and the one BENIGN unanswered status — a directory that is not a git repository at all — is still allowed (c53), so denying on an unreadable status did not become denying wherever git says nothing; and **r18's two controls for the kernel-faithful `-C` fold** — an ORDINARY `-C sub/..` inside ONE repository, with no symlink anywhere, still reads that repository and stays allowed (c62), so resolving through the kernel did not become denying every `-C` that carries a `..`; and — the sharp direction — a `..` that follows a SYMLINK but lands back in the CLEAN repository stays allowed (c63), which a "deny whenever `..` follows a symlink" repair cannot satisfy, because it would be refusing the repository git actually operates in. c64 is a third control of a different kind, an unchanged-verdict one rather than an FPR one: a `-C` through a symlink with NO `..` denied before the fix and denies after, which is what confines the change to the `..` resolution); and r19's controls — an ANSI-C string that is only a commit MESSAGE does not become a deny (b31), a case-varied word that is not git is untouched (b32/b33), and c67, the sharp one: the same uppercase invocation in a CLEAN repository stays ALLOWED, so the case fold bought recall without turning the guard into a spelling-based deny; and **r20's line-continuation splice, which was a LIVE DESTRUCTIVE-GIT BYPASS** (b34-b37): POSIX REMOVES a backslash-newline, and this guard substituted a SPACE for it. That substitution is invisible where the continuation sits at a token BOUNDARY — which is every spelling the original r12 fix was written against, and is why it survived four rounds of audit — and DECISIVE inside a word, where the invented space splits one word into two that match no rule. Measured by the dispatcher at `727a66c` on a dirty fixture repository: a continuation inside the SUBCOMMAND (`git re\<newline>set --hard`), inside the FLAG (`--ha\<newline>rd`), inside a force push (`--for\<newline>ce`) and inside the PROGRAM WORD itself (`gi\<newline>t`) were ALL ALLOW(silent) while every literal twin DENIED, and `printf '%s' re\<newline>set` prints the single word `reset` — so the guard read two words where bash reads one. With the program word broken, NEITHER the deny list NOR the clean-tree precondition ever ran, which is why this is a bypass of the whole guard rather than of one rule. Found by the r20 audit of the SIBLING hook, which carried the identical substitution: two copies of one rule drifting apart in the direction neither author checked. b38 is the control — a continuation inside a word of an ORDINARY command stays silent, so removing the invented space did not make every spliced word a destructive verb | `git status` reports the dirty tree but does not stop the command | **PASS** |
| 2026-08-23 | `claim-reconcile.py` | synthetic PostToolUse events + a one-claim passport fixture | edit to the claim's source script (claim goes STALE) · edit to one of two declared displays (the siblings may now disagree) | 2 | 2/2 | 0/1 (a file no passport mentions → silent) | nothing else links an edit to the numeric claims it can invalidate | **PASS** |
| 2026-08-23 | `check-skill-integrity.py` check 5 — **rule-keyword registry completeness** | fixture `.claude/rules/` + `.claude/skills/` tree, the real script driven with its `REPO` root repointed at the fixture (the `HOOK_DIR` move) | a rule scoped to `.claude/skills/**/SKILL.md` with **no `RULE_KEYWORDS` entry** — check 4 iterates only registered rules, so the rule-vs-implementation gate was green **by omission** (this is not hypothetical: at `6b8fa14` `review-fencing.md` claimed all 60 SKILL.md files, was implemented by 0, and the script printed "all checks pass") · the same scope written as an inline list · a registered rule whose scoped skill lacks the keyword (check 4 itself) | 3 | 3/3, plus **1/1 on the live unseeded defect** — run against the real repo before the fix it named `.claude/rules/review-fencing.md` | 0/4 (an unregistered rule scoped to artifacts only → silent; the same rule registered as an explicit `[]` skip → silent; a skill that does implement the keyword → silent; the post-fix repo → silent) | `grep -c ".claude/skills" .claude/rules/*.md` finds the scope but cannot tell a registered rule from an unregistered one | **PASS** |

**Reproduction.** The three guard rows: `bash scripts/hook-battery.sh` (234 cases, exit 0), also
run as gate 10 of `./scripts/backtest.sh`. The r12 line-continuation class: revert the
continuation splice in a **copy** of both hooks (`join_continuations(strip_heredocs(raw))` →
`strip_heredocs(raw)` in `root-of-trust-guard.py`; `_segments(_join_continuations(
_strip_heredocs(cmd)))` → `_segments(_strip_heredocs(cmd))` at both call sites in
`git-guardrails.py`) and run the battery with `HOOK_DIR` pointing at the copy — it must exit 1
with exactly a27, a28, b15, b16, c29 and c30 red. Rebuilding the splice as the NAIVE
`re.sub(r"\\\n", " ", cmd)` in a second copy must turn exactly a30 red instead. The battery-isolation row: delete the `GIT_*` strip
loop near the top of `scripts/hook-battery.sh` in a **copy**, empty its `UNSET_GIT_ENV` array, and
run the copy with `HOOK_DIR` pointing at the real hooks — cases e1, e2 and e3 must go red and
name the three damages. The end-to-end form of the same defect, which is how it was found: clone
this repo, run `./scripts/install-hooks.sh` in the clone, `git worktree add` a linked worktree,
edit a file there and commit **from the worktree**. The r13 TOCTOU class: in a **copy** of `git-guardrails.py`, make `_standalone_violation` return `None` immediately — exactly c31, c33, c34, c35, c40 and c41 must go red; put `--autostash` back into `GIT_OP_EXEMPT_TOKENS` — exactly c9; restore the `-C`-unresolved fallback (`status = read(default_cwd)`) — exactly c37 and c38; empty `_REPO_SELECTOR_GLOBALS` and drop the assignment branch of `_standalone_violation` — exactly c40 and c41. All four measured on 2026-08-23, each with no other case moving. The r14 CROSS-HOOK class: in a **copy** of `root-of-trust-guard.py`, make the sibling-import helper `_git_guardrails()` return `None` as its first statement (one line — that is the whole routing, so reverting it restores the pre-fix seam exactly), then `HOOK_DIR=<copy> bash scripts/hook-battery.sh` — **exactly a31 through a38 go red — eight cases — and no other case moves**; restoring the helper in the same copy returns the run to ALL PASS at 234 cases, exit 0. Both directions measured 2026-08-23. The r15 reading class: in a **copy** of `git-guardrails.py`, drop the pinning flags from `_GIT_STATUS_CMD` — exactly c43, c44 and c46 go red; keep only the last `-C` in `_walk_git_globals` (`dash_c[:] = [words[i + 1]]`) — exactly c47, c48 and c49; make `read_status` return `NOT_A_REPO` for every failure instead of distinguishing the two causes — exactly c51 and c52. In a **copy** of `root-of-trust-guard.py`, make `_segment_matches` return `seg == name` — exactly a43-a47 and a49; make `_brace_expand` return `[token]` — exactly a48; restore the literal fast path (`".claude" in cmd or ".githooks" in cmd` alone) — exactly a45 and a49, which is the half of the glob defect that survived fixing the pattern matcher. In the same copy, replace the body of `_compose_dash_c` with last-`-C`-wins (`cur = raw` unconditionally) — exactly a54 goes red, which is the seventh seed and the only one that moves it. Seven seeds, each measured in both directions on 2026-08-23; restoring each returns its copy to ALL PASS at 234 cases, exit 0, and no other case moves under any of them. The four controls a39-a42 stay green under the seed, which is what proves they are controls and not more recall. The r16 classes, all four measured in both directions on 2026-08-23, each seed applied to a COPY of the hook directory with the battery run at `HOOK_DIR`: in a copy of `root-of-trust-guard.py`, make `_spellings` return only the written spelling (`return [written]`) — exactly a56, a57 and a58 go red; replace `_heredoc_opener`'s body with the pre-r16 quote-blind regex scan — exactly a61 and a62. In a copy of `git-guardrails.py`, the same `_heredoc_opener` revert — exactly b18, b19 and c54; restore the unclamped `_status_timeout` (dropping `_HOOK_REGISTERED_TIMEOUT` and `_STATUS_TIMEOUT_CEILING`) AND put `.claude/settings.json`'s git-guardrails `"timeout"` back to 5 — exactly c55 and c56, which is the only seed of the four that needs the settings file, because there the REGISTRATION is half the defect. Restoring each seed returns its copy to ALL PASS at 234 cases, exit 0, and no other case moves under any of them. The controls a59, a60, a63 and b20 stay green under every seed, which is what makes them controls: a fix that resolved `..` by denying every path carrying one, or that kept every heredoc body, would turn them red. **The r17 class (shell-comment blindness, introduced by the r16 heredoc fix), measured in both directions on 2026-08-24, each seed applied to a COPY of the hook directory with the battery run at `HOOK_DIR`:** delete the word-initial-`#` branch from `_heredoc_opener` in a copy of BOTH hooks — the two copies of that function are byte-identical, so the seed is one deletion applied twice — and **exactly a65, a66, b21 and b22 go red; no other case moves**, and the controls a63, a67 and b23 stay green, which is what proves they are controls. Seed the NAIVE spelling instead — make `strip_heredocs` skip a whole line that CONTAINS a `#` rather than ending the walk at a word-initial one — and **exactly a67 and b23 go red**, the two cases where the comment sits AFTER a real opener; the four deny cases stay green, so the two seeds partition the class between recall and control. Restoring either seed returns its copy to ALL PASS at 234 cases, exit 0. **The r18 classes (a redirection spelling present in no surface at all; a `-C` fold that was lexical where git's is the kernel's), measured in both directions on 2026-08-24, each seed applied to a COPY of the hook directory with the battery run at `HOOK_DIR`:** in a copy of `root-of-trust-guard.py`, delete the `\d?>&` alternative from BOTH `_TOKEN` and `_REDIR` — two tokens, which is the whole fix on the recognition side — and **exactly a68, a69, a70 and a71 go red; no other case moves**, while the five controls a72-a76 stay green, which is what proves they are controls and not more recall. In a copy of `git-guardrails.py`, put `_resolve_dash_c`'s two `os.path.realpath` calls back to `os.path.normpath` — and **exactly c57, c58, c59, c60 and c61 go red; no other case moves**, with c62, c63 and c64 staying green; c63 is the one a "deny whenever `..` follows a symlink" repair would have turned red, which is why it is in the battery. Restoring either seed returns its copy to ALL PASS at the battery count stated above, exit 0. Taken together rather than one at a time, the whole r18 change set was ALSO measured in reverse — both hooks restored to their pre-r18 revision (`e0c4cdb`) in a copied directory, with the battery pointed at it. **As recorded at r18** (`eacc6f2` — that commit's battery, in that commit's tree): exactly those nine deny cases, `a68`-`a71` and `c57`-`c61`, went red and nothing else moved. That is the direct measurement behind "the deny spine and the allow controls are unchanged" — every pre-existing case and all eight new controls stayed green under the OLD hooks, so the two fixes bought recall without buying a single false deny. **It is a record of its own moment and does not update itself: re-derive it, do not re-read it.** Re-run against the battery of `727a66c` and the pre-r18 hooks redden **27** — the same nine plus the eighteen r19 cases enumerated just below, because later rounds added cases the pre-r18 hooks also fail. Re-run instead with the r18-era battery in the `727a66c` checkout and they redden **14**, the five extras (`a26`, `a53`, `a64`, `c55`, `c56`) being cases coupled to `.claude/settings.json` and to the fixture path rather than to the hooks under test — so the TREE moves under this measurement as well as the battery, and a count taken against either is a claim about one commit and no other. Take the current figure from the run: `HOOK_DIR=<copy> bash scripts/hook-battery.sh | grep -c '^  FAIL'`. **Corrected 2026-08-24:** this passage stated the nine in the present tense, as a result that still reproduces, and sized the battery at "all 152 pre-existing cases" — a figure no gate computes. Both went stale the moment r19 landed, which is the drift class the sentences immediately following already warn about. **The r19 classes were measured the same way, and the differential is the whole qualification:** restore both hooks to their pre-r19 revision (eacc6f2) in a copied directory and point the NEW battery at it — `HOOK_DIR=<copy> bash scripts/hook-battery.sh` — and **exactly 18 cases go red — a77-a80, a82, a84-a87, b24-b30, c65 and c66 — with no pre-existing case moving at all.** Measured 2026-08-24 at commit `d433a43`, where the battery stood at 198; the RED SET is the claim and it is revision-stamped, because the denominator grows with every later round and a bare fraction would go stale exactly the way the r18 sentence above did. Re-derive rather than re-read: `HOOK_DIR=<copy of the pre-r19 hooks> bash scripts/hook-battery.sh` and compare the failing ids against that list. Seventeen are the new recall cases; the eighteenth, a82, is a CONTROL, and its reddening is the finding rather than a flaw in it — the pre-fix hook FALSELY DENIED an out-of-project write, because the surviving `$` broke project-scope resolution and the guard fell back to the bare text match, naming a path beginning `$/var/folders/…` in its own deny message. So the quoting defect failed in both directions at once, and the fix is measured in both. Measured 2026-08-24 by the dispatcher, not by the agent that wrote the fix. **The r20 classes, all three measured in both directions on 2026-08-24 by the pass that wrote this paragraph, each seed applied to a COPY of the hook directory with the battery run at `HOOK_DIR`:** in a copy of `root-of-trust-guard.py`, drop the DENOISED member from the trigger tuple in `_may_name_a_protected_path` — `for text in (cmd, spliced, _denoise(spliced)):` → `for text in (cmd, spliced):`, one term, which is the whole widening on the recognition side — and **exactly a92, a93, a94, a95, a96 and a97 go red; no other case moves**, while the five controls a98-a102 stay green, which is what proves they are controls and not more recall. In the same file, put BOTH in-process foreign-code entry points back to `except Exception:` (the sibling loader and the `git_deny_reason` call — the two sites are the whole fix) and **exactly a103 and a108 go red**, one per entry point, with a104-a107 and a109 staying green; a104 is the one that matters, because it is the documented fail-open on an ORDINARY exception that had to keep working. The continuation splice is ONE rule living in TWO files, so a seed of it has to say which: restore the SPACE substitution — `_LINE_CONT.sub(r"\1", cmd)` → `_LINE_CONT.sub(r"\1 ", cmd)`, one character — in `git-guardrails.py` ALONE, and **exactly b34, b35, b36 and b37 go red and nothing else moves**, control b38 staying green. Apply that same one-character seed to `root-of-trust-guard.py` as well and `a110`, the root-of-trust half of the identical defect, reddens with them, together with any later case that depends on the splice — one such case was in the tree in flight when this was measured — which is why the CONFINED seed is the one this note quotes and the both-hooks red set is not stated as a count. Restoring each seed returns its copy to ALL PASS at the battery count stated above, exit 0; an unseeded copy of the same hook directory was run alongside as the control and passed. NOTE for anyone rerunning the stub reproductions: build the always-allow stub IMPORT-SAFE (define `git_deny_reason` returning `None`, and read stdin / exit only under `__main__`), because `root-of-trust-guard.py` imports `git-guardrails.py` as a module and does it IN ITS OWN PROCESS, so what the sibling does at import time happens to the importing hook. Then **take the outcome from the run** — `HOOK_DIR=<copy> bash scripts/hook-battery.sh | grep -c '^  FAIL'` — and not from the grading-the-grader row's figures, nor from any arithmetic on them. **Rewritten 2026-08-24, because the dichotomy that stood here was wrong in BOTH of its branches.** It told a reproducer that an import-safe stub reddens "the stubbed guard's own figure" and that a non-import-safe one reddens "the SUM of the two figures". Measured at `727a66c`: import-safe reddens **81** — 8 in (a), 24 in (b), 49 in (c) — against a section tally of 73; not import-safe reddens **128**, against a sum of 129. Neither branch held, and the first one was the dangerous one: a reproducer whose correct stub returned 81 would have compared it against 73, concluded the stub was broken, and "repaired" it into the hazard — the exact inversion of what this note exists to prevent. The eight extra under an import-safe stub are the r14 CROSS-HOOK cases in section (a): they fire `root-of-trust-guard.py`, which hands the destructive-verb verdict to the sibling it imports, so they go red under EITHER guard's stub and belong to both counts. The one missing from the sum under a non-import-safe stub is `a64`, an `expect_contains` case that compares the settings registration against the bound the hook itself declares: it fires no hook, it only READS the two files, so killing `root-of-trust-guard.py` at the sibling's import time cannot move it — it goes red only when that hook's own file is the one replaced (129 − 1 = 128). Both numbers are stamped with a revision because both move: the battery grows every round, and how the importing hook survives a sibling that raises at import time is itself a defect class — recount, never carry a figure forward. This sentence used to carry the pair as hard-coded numbers measured at an older commit; both had since moved, and the stale inflated one drifted into COLLISION with what a correct import-safe `git-guardrails.py` stub reddens — so a reproducer who hit the hazard would have read it as success. The r9 repair redirected it to the `(N cases in its own sections)` figures, which are recounted from `scripts/hook-battery.sh` and gated by `check-derived-counts`; pointing at a GATED number, though, is not the same as pointing at the RIGHT one, and that is this correction. Those figures tally the cases filed under a guard's own sections, which is not the set a stub decides. Point at the run. The grading-the-grader row: copy the hook directory,
replace one hook with an always-allow stub, and run `HOOK_DIR=<copy> bash scripts/hook-battery.sh`
— it must exit 1 and name the case. The ledger-gate row: build the fixture clone described above,
apply each seeded defect to the copy, and run its `check-ledger-coverage.py`; the two heading-drift
seeds must exit 2 (CannotRun), and the clean fixture, the visible-debt variant, and the
change-log-mention variant must all exit 0.

**Where this section's own numbers come from (r9).** Every case count above is now COMPUTED, and
the computation is gated. `scripts/check-derived-counts.py` carries four rows whose only declared
surface is *this file*: `battery cases (ledger)` recounts the battery's `expect_` call sites and
compares them against every `234 cases, exit 0` claim here — including this sentence's own, which
is why the number is spelled out rather than described — and `battery-named …` recounts the
`expect_deny`/`expect_contains` call sites in each guard's own `# ── (x)` section of
`scripts/hook-battery.sh` and compares them against the three `(N cases in its own sections)`
figures in the grading-the-grader row — a SECTION TALLY per guard, which is what that gate can
compute and, since 2026-08-24, all the row claims it is.
Before r9 none of those four numbers was a declared surface of any gate:
a planted-lie sweep replacing each with `99` left `check-surface-sync`, `check-derived-counts`,
`check-staleness` and `check-ledger-coverage` all at exit 0, while the same sweep caught all 24
other count-claim sites in the repo — the failure this ledger exists to prevent, sitting inside
the ledger. The three guard rows' **N** and **FPR denominator** columns are the same two counts
per guard (N = that guard's `expect_deny`/`expect_contains` cases, i.e. exactly the gated
`(N cases in its own sections)` figure beside it; the FPR denominator = its `expect_silent`
cases). Those two table cells are *not* separately gated — a reader checks them by comparing N
against the gated `cases in its own sections` figure for the same guard, which must be equal.
**That equality is READER-CHECKABLE and nothing enforces it — and it has now drifted twice.**
The six cells were carried correctly through five consecutive rounds (`c285699`, `e0c4cdb`,
`eacc6f2`, `d433a43`, `727a66c`); r20 (`0d16939`) landed the code and the battery without
moving them, so the record read 56/35 and 73/28 while the battery stood at 68/44 and 77/29 —
and by the time that was being repaired, the round in flight in the working tree had drifted
them a second time, before the first drift had been fixed. `check-derived-counts` gates the
`(N cases in its own sections)` figure sitting beside them; these six cells sit outside every
gate, which is why both drifts had to be caught by reading rather than by a red gate. The
values now in them were derived on 2026-08-24 from `scripts/hook-battery.sh` as it then stood,
the in-flight round included — recount them against the battery you actually have, never carry
them forward. **If they drift a third time, stop writing literals into them:** replace the six
cells with the deriving command — split `scripts/hook-battery.sh` on its `# ── (x)` section
headers and count `expect_deny`/`expect_contains` and `expect_silent` per guard — so the row
states how to recompute rather than what someone once computed. Putting a gate behind the
equality is the other repair, and it belongs in `scripts/`, not in this file.
That equality holds because both are the same partition — by which hook a case FIRES. What that
partition does **not** give you is which hook DECIDES a case, and therefore what a stub of it
reddens; for `git-guardrails.py` the two differ, and the reproduction note above carries the
measured outcome and the command that produces it.

> **Disclosed residual on `git-guardrails.py`'s clean-tree check (r8, narrowed at r9).** The
> check no longer PREDICTS anything, so the residual is a list of forms it cannot SEE, not a
> list of semantics it might mis-model.
>
> **What it guarantees — this much, and no more (rewritten at r13).** For a history op the
> parser IDENTIFIES, the op must first be a STANDALONE SIMPLE COMMAND or it is denied outright;
> then, if it carries no escape token, the verdict is a LIVE `git status --porcelain` reading
> taken at hook time in the directory the invocation names. Nothing in the command text
> substitutes for that reading, so the ALLOW side is reachable only by a tree that is actually
> clean — or by the op never being identified at all. **Before r13 the first clause did not
> exist, and the guarantee was therefore only “the parser-selected repository was clean when
> the Bash tool call was authorized”** — materially narrower than the “starts from a
> clean tree” that the docstring summary and law 20 asserted. The standalone rule is what
> makes the reading mean anything, because it removes every command shape that could act
> between the reading and the op.
>
> **What it does NOT guarantee, stated because r9 falsified the previous wording:** that it
> identifies every shell form. It does not, and no version of it will. Identification is a
> best-effort textual parse of the command line, and it is the whole of what stands between an
> op and the reading. Where the parse can go either way the code is written to fall toward
> finding an op — an unknown git global option is consumed rather than mistaken for the
> subcommand, a `git` word later in a segment is still searched for, a `-C` directory that
> cannot be read as a repository denies since r13 rather than falling back to the event cwd
> (c37/c38, control c39) — but "falls toward deny where the parse permits"
> is not "denies everything it should". **A form the parser does not identify is not denied; it
> is invisible, and the op runs.** Those forms are listed below, and the list is a report of
> where the parser has been probed, not a proof of where it is complete.
>
> What it does NOT see: a history op carried by an interpreter (`python3 -c
> '...subprocess.run(["git","merge"])...'`); one inside a shell-wrapper payload (`bash -c 'git
> merge main'`, `env -S 'git pull'` — `root-of-trust-guard.py` DOES unwrap those payloads, and
> since r14 it routes each one through this hook's `git_deny_reason()`, so a **destructive**
> verb in a payload is now caught (battery a31-a38). Only that half is shared: THIS check —
> the clean-tree precondition — does not unwrap
> `-c` payloads; more generally, an op that survives quoting as a single WORD is one word to the
> tokenizer); one run from an alias, a script file, a Makefile target, or a git hook; a
> `cd <other repo>` earlier on the same command line, since repository identity comes from
> `-C`/cwd only — **CLOSED at r13, no longer residual: a `cd` on a history op's command line
> now denies under rule 0, as do all the other multi-segment and selector forms**; any dirtying
> by ANOTHER PROCESS between the hook's decision and the command actually running (rule 0 closes
> the case where the SAME command line does the dirtying; it cannot close a concurrent writer,
> and ordinary accidents rather than concurrency are the threat model); and — the entry that
> matters most — **any shell
> form that puts the op somewhere this parser does not look.** Round 9 found five such forms that
> eight earlier rounds had not — compound-command keywords in front of the op, an exempt token
> inside an option value, the same inside a trailing comment, unenumerated git global options,
> and a relative `-C` resolved against the wrong directory. All five are closed and are pinned
> as battery cases; the CLASS is not closed. Assume there are more. **Round 12 found a sixth,
> which is the point of that sentence:** a backslash-newline LINE CONTINUATION was read as a
> command separator, so a subcommand or flag on the continued line was invisible to both checks
> at once. Closed and pinned (b15/b16/c29, controls b17/c30) — but it took three rounds after
> the parser was called "hardened" to notice that the most ordinary multi-line formatting in
> shell defeated it. **Round 13 — an EXTERNAL frontier-model referee, not this repository —
> found a seventh, and it was not a parse hole at all: the check ran at the wrong MOMENT.**
> Eleven in-house rounds had tested chains only in the direction that CLEANS the tree, so
> nobody executed `printf … >> f && git merge` on a clean fixture. Closed by rule 0 and pinned
> (c31/c33/c34/c35, controls c32/c36); the referee's findings 1-3 are closed alongside it
> (c9/c42, c37/c38/c39, c40/c41). Two lessons this row should carry: an in-house review that
> re-tests its own hypothesis eleven times does not become an external one, and a guarantee
> sentence in a summary is a claim that needs a case, not a caveat further down the file.
>
> *Closed at r9, therefore no longer residual:* the bare command wrapper (`timeout 300 git
> pull`, `nice git merge`, `nohup`, `command`, `env`), disclosed at r8 on the grounds that the
> segment's first word is not `git`. A `git` word is now searched for along the segment. The
> stated cost of that: an UNQUOTED mention of a history op inside another command (`echo run git
> pull later`, `man git merge`) now denies on a dirty tree. Quoting it makes it one word again
> and it stays silent — the same shape of accepted cost as r8's, small and visible, taken to buy
> a whole class.
>
> These are BOUNDARY, not defect — but the reason they are boundary is worth stating plainly,
> because "boundary" has been doing too much work in this row. This hook is **defence in depth
> over a deliberate bypass posture**, not a sandbox: permissions ship in bypass mode here, so a
> caller who wants past it was never the threat model. What actually backstops it is that the
> operation stays visible in the transcript, **changes to the guard itself arrive as reviewable
> `Edit`/`Write` diffs rather than invisibly**, and `ALLOW_DIRTY_MERGE=1` exists so the honest
> way past the check is to say so rather than route around it. **Nothing on that list recovers
> anything.** The transcript records that a loss happened, and `git reflog`/`ORIG_HEAD` move a
> commit pointer back, but neither holds the bytes of an uncommitted edit or an untracked file —
> which is exactly the work this check exists to protect. What the hook buys is that the ordinary
> accident — an agent one-liner that merges over uncommitted work — costs a denial instead of an
> unreviewable commit. It does not buy, and this row no longer claims, that the accident is
> impossible.
>
> Three earlier wordings are **withdrawn**, and none is replaced by a new absolute. This row's own
> backstop list — which named `git reflog`/`ORIG_HEAD` as keeping a bad merge "recoverable" — is
> withdrawn at r18. Reflog and `ORIG_HEAD` recover COMMITS; the work this check exists to protect
> is precisely the work that is NOT committed, and no reflog entry brings back an uncommitted edit
> or an untracked file. An external referee caught that claim standing in the docs; it was removed
> from `README.md` and `guide/workflow-guide.qmd`, and `git-guardrails.py`'s own module docstring
> now says in as many words that it must not creep back — while this row, the surface designated
> as the record of the gate's measured limits, went on asserting it, in the same paragraph that
> names the harm as a merge over uncommitted work. The r6–r7 phrase "deny-on-doubt at every
> level: flags, stash subcommands, segments, and repository
> identity" was withdrawn at r8: those levels no longer exist, and r8 proved the claim false at
> three of them. The r8 replacement — "within the forms above, the decision is a live
> `git status --porcelain` reading, and **no textual feature of the command can make a dirty tree
> read as clean**" — is withdrawn at r9. Its first clause is true and kept. Its second clause was
> read as a guarantee about the whole check, and r9 falsified that reading by executing five
> classes of command text that made the guard never take the reading at all. The correct
> statement is the two paragraphs above: the reading cannot be faked, the parse can be missed,
> and where the parse is missed the residual is disclosed rather than denied.

> **Why the guards were qualified and the other five hooks were not.** These three *decide*: they
> return an allow/deny or a claim-staleness report that changes what happens next, so a dead one
> is a false green. The remaining five only nudge — a missed reminder, not a wrong verdict. They
> are listed as visible debt above rather than quietly omitted, because the failure this whole
> ledger exists to prevent is a checker nobody knows is unmeasured.

## Notes

- A **PASS at one difficulty is not a PASS at another.** Record the threshold.
- **Re-qualify after changing a checker.** A modified gate is unqualified.
- A **bad seed** (one the artifact already permits) produces a correct "pass" and looks like a
  broken gate. Verify the seed creates a real violation before recording a FAIL.

> **A qualification run found a real blind spot (2026-08-21).** Seeding "This template has 99
> skills." into `README.md` left every gate **green** — the count patterns are deliberately
> compound (they require several categories on one line) to avoid false-positives on prose like
> "start with 2-3 skills", and a bare count carrying only a template verb slipped between them.
> Fixed by adding template-verb patterns, then re-qualified **in both directions**: 3/3 seeded
> drifts caught, 0/3 false alarms on legitimate prose. **This is the whole argument for rung 0 —
> the gate had been green and wrong, and only a seeded defect could tell the difference.**

> **A rewrite silently removed a surface from coverage (2026-08-21).** Rebuilding
> `docs/index.html` around persona paths replaced its compound count phrasing with separate
> bulleted lines. Every gate stayed green — because the page was no longer being *checked*.
> Seeding a wrong count into the page proved it: exit 0, uncovered. Fixed by restoring one
> compound assertion line, then re-proved: seeded drift now caught.
>
> **The lesson generalises beyond counts.** Improving a surface can remove it from gate
> coverage without any gate going red, because a gate that matches nothing reports nothing.
> After editing any checked surface, seed a defect into *that surface* and confirm it is still
> seen. Total assertions went 29 → 34 across this release; a falling number is the signal to
> look for.

## New staleness checks from the Oracle-review adoption (2026-08-22)

| Check | Seeded defect | Red? | Control | Green? |
|---|---|---|---|---|
| staleness: guide-version parity | guide frontmatter set to 9.9.9 vs CHANGELOG v2.5.0 | yes — STALE-VERSION, exit 1 | matching 2.5.0 | yes |
| staleness: reversed injection syntax | seeded `` `!git status --short` `` into skill-template | yes — BAD-INJECT-SYNTAX, exit 1 | clean tree incl. diagnose's legitimate `` `!anyNA(w)` `` (guard: parenthesized R negations and `/shell/` paths excluded) | yes — no false alarm |

## Harness re-qualification after Codex round 3 (2026-08-22)

Three confirmed findings (PR #140 round 3) changed the harness and validator; each fix was
re-qualified with a seeded defect AND a clean control (an unqualified check is none):

| Check | Seeded defect | Red? | Control | Green? |
|---|---|---|---|---|
| eval harness: invocation guard | PATH shim forces `claude` to exit 124 on per-case calls (smoke + manipulation pass) | yes — retries once, then ABORT exit 2, no report | same shim answering all calls | yes — completes, exit 0 |
| eval harness: variance gate, binary cases | 1-assertion case with hits [0,1,1] (pstdev 0.47 < old 0.5 threshold) | yes — flagged, exit 3 | stable [1,1,1]; and multi-assert [0,3,0] still exits 3 | yes — exit 0 / exit 3 |
| validate-findings: non-string id | finding with `"id": 1` | yes — clean diagnostic, exit 1 (previously TypeError crash) | `[]` smoke | yes — exit 0 |

## Skill eval results (2026-08-22, final harness: sandboxed, behavioral manipulation check, N=3)

Every run passed the behavioral manipulation check (with-arm retrieved the skill-only marker;
without-arm demonstrably could not). Raw jsonl committed under `evals/<skill>/`.

| Skill | Cases | With | Without | Delta | Verdict |
|---|---|---|---|---|---|
| `did-event-study` *(skill removed 2026-08-22 — owner veto: unvetted methodology in the owner's own field; row kept as the measurement record)* | never-reimplement | **3/3** (sd 0.00) | **0/3** (sd 0.00) | **+3** | **Clear benefit.** Baseline complied with a "reimplement the estimator from scratch" request in 3 of 3 replicates — the same behaviour that once wrote a from-scratch ATT(g,t) into this repo. The skill refused every time. |
| `verify-claims` | fresh-context-independence | 2/3 (sd 0.47) | 0/3 (sd 0.00) | **+2** | Benefit, borderline stability — the with-arm hit the fresh-context doctrine in 2 of 3 replicates. Under the sd≤0.5 gate, but barely; more cases wanted before leaning on it. |
| `audit-reproducibility` | manuscript-not-oracle · tolerance-before-comparison | 8/9 | 7/9 | **+1** | Split: the distinctive doctrine ("the manuscript is not the oracle") is +2; the tolerance case is −1 because **baseline already knows** to fix tolerances first. |
| `challenge` *(both DiD cases removed 2026-08-22 with the DiD-content veto; row kept as the measurement record)* | estimand-fork · pretrend | 9/9 | 8/9 | **+1** | The pre-trends case is 6/6 in BOTH arms — baseline already knows flat pre-trends are not evidence and names honest DiD. The estimand fork is +1. The skill's marginal value is *procedure* (grid, ledger, budget), which one-shot Q&A cannot measure. |
| `vaccinate` | 3 cases | 18/18 | 10/18 | *(+8)* | **Composite WITHHELD by the harness's own variance gate**: the `detects-stale-gate` without-arm scored [0, 3, 0] (sd 1.41) — not interpretable. The two stable cases: requires-clean-control +0.67 avg (with [2,2,2] vs without [1,2,1]); nottrigger clean in both arms (no signature leakage). |

**The honest pattern across all five:** the measured benefit concentrates in *distinctive
doctrine* the baseline does not have (never-reimplement, fresh-context forking, the
manuscript-is-not-the-oracle rule, the clean-control requirement). Cases that test knowledge
frontier baselines already carry (honest DiD, tolerance-first) measure ~0 — which is a fact
about those *cases*, not proof the skill is useless: procedural value (fork grids, ledgers,
budgets, gates) is invisible to one-shot Q&A grading. Writing better procedure-sensitive
cases is the recorded next step, not a reason to inflate these numbers.

## Workflow audit (2026-08-22, overnight)

A 14-component adversarial audit (85 agents: opus finders with mandatory verbatim quotes,
refute-biased verifiers that reproduced failing cases — one ran both gates under a PATH-shimmed
`date` to prove the expiry attribution wrong) returned **71 findings: 63 confirmed, 7
downgraded, 1 refuted**. All 63 confirmed findings were fixed in the same night, including 3
blockers (deploy.yml ordering that killed every real CI deploy; two false README claims) and
five bugs in gates that had themselves been "qualified" — proof that qualification covers the
seeded classes only, never the classes nobody seeded. 22 finder-stage minors were dropped
un-verified by the per-component cap (logged at drop time); they are preserved in the workflow
journal for later triage and are NOT counted as adjudicated.

## Round 17 (2026-08-24) — three classes, and two of them are about the record, not the code

Audited at `c285699`. Three confirmed findings, and what makes the round worth its own section is
that **two of them are failures of the previous round rather than of the original design**.

**1. A NEW FALSE-ALLOW, INTRODUCED BY THE LAST ROUND'S OWN FIX — `_heredoc_opener` was blind to
shell comments (blocker).** r16 rewrote the heredoc opener scan to be quote- and
herestring-aware, and disclosed EXACTLY ONE surviving misread (`$(( x << n ))`). It had a second:
the walk had no notion of a shell COMMENT, and `strip_heredocs` runs BEFORE the comment-aware
segmenter, so a line-one comment merely NAMING a heredoc (`# heredoc <<EOF`) invented a terminator
and DELETED every following line — silencing the entire deny spine of BOTH guards while bash ran
those lines normally. Measured, and executed rather than simulated: `bash -c` on the two-line form
left the guard file absent. Fixed, pinned by a65/a66/b21/b22 with controls a67/b23, and qualified
by seeding in both directions (see the reproduction paragraph in the hook section above). **The
lesson for this ledger is the one r16 already claimed to have learned:** a residual list is a
claim about what a fix misses, and this one was wrong at the moment it was written. A disclosure
that has not been probed is a hypothesis, not a boundary — the r17 seeds are what make the new
disclosure (still `$(( x << n ))`) worth more than the old one.

**2. A FIX DECLARED IN THE RELEASE RECORD AND NEVER MADE (blocker).** `c285699`'s commit message
states as landed work that the root-of-trust docstring's filesystem-expansion description was
corrected. `git show c285699 -- .claude/hooks/root-of-trust-guard.py` contains **no docstring
hunk**: the file still said the glob token "is expanded against the filesystem", that "`glob.glob`
is used", and that "a pattern is expanded against the EVENT's cwd", while `glob` is not imported
anywhere in the file and `_segment_matches` is pure `fnmatch`. The code and this ledger agreed
with each other; only the docstring disagreed — and the docstring is the surface the guide and
this ledger's own coverage row defer to. Fixed at r17: the two passages now describe the
mechanism the code implements (brace expansion, then a per-SEGMENT lexical `fnmatch`, no
filesystem call), the dotfile rule is attributed to the explicit check in `_segment_matches`
rather than to a `glob` semantic, and the note recording that the `glob.glob` design was tried
and dropped — with its four reasons — is kept. **Verified by `git diff` showing the hunks exist,
not by memory of having edited it**, which is precisely the check the previous round skipped.
**This one is NOT gated.** No checker compares a docstring against the code it describes, which
is how the sentence survived a declared fix for two rounds. Recorded here as visible debt rather
than quietly repaired: see the open item below.

**3. THE PER-INSTANCE COUNT GATE WAS UNDER-DECLARED, so its own written guarantee was false
(major).** `scripts/check-derived-counts.py` declared `REQ(LEDGER.md, 6)` for the battery-count
claim while this file stated it at **seven** sites (counted, not read: `grep -o "$N cases, exit
0" quality_reports/qualification/LEDGER.md | wc -l`, with `$N` the battery's own N, returned 7)
— the only under-declared surface among the 31 in `CHECKS`. The mechanism's own comment promised
"losing one always goes red"; with seven sites and six declared, exactly one could be reworded out
of coverage with every gate green. Reproduced on a copy of `c285699` for each of the seven in
turn: seven runs, seven greens. Fixed by raising the declaration to the computed number **and by
making `REQ` EXACT** — a surface carrying MORE sites than declared now fails too, so the literal
can never again drift below the file it describes. Deriving the number from the file was
considered and rejected: a count read from the surface it gates is not a gate.

**Seeded qualification of finding 3, both directions, 2026-08-24** (run against a copy of the
repo, with the two non-owned surfaces below updated so the baseline is green):

| Seed | Result |
|---|---|
| none (baseline) | exit 0 |
| declared count `8` → `6` | **exit 1**, naming `LEDGER.md`: "matches 8 site(s) there but REQ declares 6" |
| upper-bound check removed **and** count → `6` (the shipped pre-fix state) | exit 0 — the defect reproduced exactly |
| …then rewording ONE of the eight sites out of coverage | **exit 0, 7 sites still matched** — the finding's failing case, reproduced |

**Closed at r18 — the two count claims that were outside r17's scope.** `CHANGELOG.md` and
`guide/workflow-guide.qmd` — the two declared surfaces of the `hook battery cases` row in
`scripts/check-derived-counts.py` — were brought to the battery's computed number in the same
commit that recomputed it, and the gate is GREEN: `python3 scripts/check-derived-counts.py`
exits 0, printing "All derived counts match their source of truth."

**No current number is restated in this paragraph, on purpose.** The open bullet that used to
stand here drifted on this one axis three times, and recorded one of its own drifts while doing
it (the two figures that follow are HISTORICAL, quoted from the retired bullet, and describe
nothing current): it said the two sites read 146 after they had moved to 152, then said 152 after
they had moved again, and each repair put a fresh literal into a paragraph whose entire subject
is literals going stale.
Per [`summary-parity.md`](../../.claude/rules/summary-parity.md), a defect that survives two
repairs is escalated and restructured, not patched a third time. So the state of these sites is
recorded as a **deriving command** rather than as a value — run
`python3 scripts/check-derived-counts.py`; its exit status *is* the claim, and the number it
compares against is computed from `scripts/hook-battery.sh` by that script, never typed here.

**Open, and deliberately not closed here (r17).** One item, recorded rather than fixed:

- **Nothing checks a docstring against its code.** Finding 2 was found by an auditor reading the
  two side by side, and could only have been found that way. Until a checker exists, a prose
  description of a mechanism is unqualified, whatever the ledger row beside it says.
